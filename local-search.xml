<?xml version="1.0" encoding="utf-8"?>
<search>
  
  
  
  <entry>
    <title>在 PHP 中使用单元测试</title>
    <link href="/ef77815d.html"/>
    <url>/ef77815d.html</url>
    
    <content type="html"><![CDATA[<h1 id="相关概念"><a href="#相关概念" class="headerlink" title="相关概念"></a>相关概念</h1><ul><li><code>单元测试</code><br>单元测试是针对程序的最小单元进行测试，方法、类等都可以是一个单元，根据实际情况判定，一般指的是方法。</li><li><code>断言</code><br>在程序设计中，断言（assertion）是一种放在程序中的一阶逻辑（如一个结果为真或是假的逻辑判断式），目的是为了标示与验证程序开发者预期的结果－当程序运行到断言的位置时，对应的断言应该为真。若断言不为真时，程序会中止运行，并给出错误消息。</li><li><code>PHPUnit</code><br>是一个轻量级的 PHP 测试框架</li></ul><h1 id="PHPUnit-的安装和配置"><a href="#PHPUnit-的安装和配置" class="headerlink" title="PHPUnit 的安装和配置"></a>PHPUnit 的安装和配置</h1><p>目前支持的版本是PHPUnit 9</p><h2 id="安装"><a href="#安装" class="headerlink" title="安装"></a><a href="https://phpunit.de/getting-started/phpunit-9.html" target="_blank" rel="noopener">安装</a></h2><h3 id="PHP-档案包（PHAR）"><a href="#PHP-档案包（PHAR）" class="headerlink" title="PHP 档案包（PHAR）"></a>PHP 档案包（PHAR）</h3><pre><code>➜ wget -O phpunit https://phar.phpunit.de/phpunit-9.phar➜ chmod +x phpunit➜ ./phpunit --versionPHPUnit 9.0.0 by Sebastian Bergmann and contributors.</code></pre><h3 id="Composer"><a href="#Composer" class="headerlink" title="Composer"></a>Composer</h3><pre><code>➜ composer require --dev phpunit/phpunit ^9➜ ./vendor/bin/phpunit --versionPHPUnit 9.0.0 by Sebastian Bergmann and contributors.</code></pre><h2 id="配置"><a href="#配置" class="headerlink" title="配置"></a>配置</h2><h3 id="XML-配置文件"><a href="#XML-配置文件" class="headerlink" title="XML 配置文件"></a><a href="https://phpunit.readthedocs.io/zh_CN/latest/configuration.html" target="_blank" rel="noopener">XML 配置文件</a></h3><p>用于编排测试套件，示例如下：</p><pre><code class="hljs xml"><span class="hljs-tag">&lt;<span class="hljs-name">phpunit</span> <span class="hljs-attr">bootstrap</span>=<span class="hljs-string">"./bootstrap.php"</span></span><span class="hljs-tag">         <span class="hljs-attr">colors</span>=<span class="hljs-string">"true"</span></span><span class="hljs-tag">         <span class="hljs-attr">verbose</span>=<span class="hljs-string">"true"</span></span><span class="hljs-tag">&gt;</span>    <span class="hljs-tag">&lt;<span class="hljs-name">php</span>&gt;</span>        <span class="hljs-tag">&lt;<span class="hljs-name">ini</span> <span class="hljs-attr">name</span>=<span class="hljs-string">"error_reporting"</span> <span class="hljs-attr">value</span>=<span class="hljs-string">"-1"</span>/&gt;</span>    <span class="hljs-tag">&lt;/<span class="hljs-name">php</span>&gt;</span>    <span class="hljs-tag">&lt;<span class="hljs-name">testsuites</span>&gt;</span>        <span class="hljs-tag">&lt;<span class="hljs-name">testsuite</span> <span class="hljs-attr">name</span>=<span class="hljs-string">"test"</span>&gt;</span>            <span class="hljs-tag">&lt;<span class="hljs-name">directory</span>&gt;</span>./Tests/<span class="hljs-tag">&lt;/<span class="hljs-name">directory</span>&gt;</span>        <span class="hljs-tag">&lt;/<span class="hljs-name">testsuite</span>&gt;</span>    <span class="hljs-tag">&lt;/<span class="hljs-name">testsuites</span>&gt;</span><span class="hljs-tag">&lt;/<span class="hljs-name">phpunit</span>&gt;</span></code></pre><h3 id="Bootstrap-启动文件"><a href="#Bootstrap-启动文件" class="headerlink" title="Bootstrap 启动文件"></a>Bootstrap 启动文件</h3><p>在启动 PHPUnit 时会加载的文件，用于自动加载依赖等，示例如下：</p><pre><code class="hljs php"><span class="hljs-meta">&lt;?php</span>$classNameMap = [    <span class="hljs-string">'single'</span> =&gt; <span class="hljs-keyword">__DIR__</span> . DIRECTORY_SEPARATOR . <span class="hljs-string">'single'</span>,];spl_autoload_register(<span class="hljs-function"><span class="hljs-keyword">function</span><span class="hljs-params">($className)</span> <span class="hljs-title">use</span> <span class="hljs-params">($classNameMap)</span> </span>&#123;    $position = strpos($className, <span class="hljs-string">'\\'</span>);    <span class="hljs-keyword">if</span> ($position) &#123;        $namespace = substr($className, <span class="hljs-number">0</span>, $position);        $directory = $classNameMap[$namespace] ?? <span class="hljs-string">''</span>;        <span class="hljs-keyword">if</span> ($directory) &#123;            $classFile = $directory . substr($className, $position) . <span class="hljs-string">'.php'</span>;            is_file($classFile) &amp;&amp; <span class="hljs-keyword">require</span> $classFile;        &#125;    &#125;&#125;);</code></pre><h3 id="在-PhpStorm-里配置使用"><a href="#在-PhpStorm-里配置使用" class="headerlink" title="在 PhpStorm 里配置使用"></a>在 PhpStorm 里配置使用</h3><ul><li>文件 -&gt; 设置 -&gt; PHP，设置 cli 解释器。</li><li>文件 -&gt; 设置 -&gt; PHP -&gt; 测试框架，设置 phpunit.phar 路径或 composer 下的 phpunit 路径， 设置 xml 配置文件。</li><li>使用 IDE 自动帮我们执行 PHPUnit 的命令，便不用手动操作了，测试时只需点击文件里的执行按钮即可。</li></ul><h1 id="编写-PHPUnit-测试"><a href="#编写-PHPUnit-测试" class="headerlink" title="编写 PHPUnit 测试"></a>编写 PHPUnit 测试</h1><h2 id="步骤"><a href="#步骤" class="headerlink" title="步骤"></a>步骤</h2><ul><li>针对类 Class 的测试写在类 ClassTest 中。</li><li>ClassTest（通常）继承自 PHPUnit\Framework\TestCase。</li><li>测试都是命名为 test* 的公用方法。</li><li>也可以在方法的文档注释块（docblock）中使用 @test 标注将其标记为测试方法。在测试方法内，类似于 assertSame()（<a href="https://phpunit.readthedocs.io/zh_CN/latest/assertions.html" target="_blank" rel="noopener">参见断言</a>）这样的断言方法用来对实际值与预期值的匹配做出断言。</li></ul><h2 id="示例"><a href="#示例" class="headerlink" title="示例"></a>示例</h2><ul><li>类<pre><code class="hljs php"><span class="hljs-meta">&lt;?php</span><span class="hljs-keyword">namespace</span> <span class="hljs-title">single</span>;<span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">test</span></span><span class="hljs-class"></span>&#123;    <span class="hljs-keyword">public</span> $attr;    <span class="hljs-keyword">public</span> <span class="hljs-function"><span class="hljs-keyword">function</span> <span class="hljs-title">hello</span><span class="hljs-params">()</span>: <span class="hljs-title">string</span></span><span class="hljs-function">    </span>&#123;        <span class="hljs-keyword">return</span> <span class="hljs-string">'hello'</span>;    &#125;&#125;</code></pre></li><li>测试类<pre><code class="hljs php"><span class="hljs-meta">&lt;?php</span><span class="hljs-keyword">use</span> <span class="hljs-title">PHPUnit</span>\<span class="hljs-title">Framework</span>\<span class="hljs-title">TestCase</span>;<span class="hljs-keyword">use</span> <span class="hljs-title">single</span>\<span class="hljs-title">test</span>;<span class="hljs-keyword">final</span> <span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">Assert</span> <span class="hljs-keyword">extends</span> <span class="hljs-title">TestCase</span></span><span class="hljs-class"></span>&#123;    <span class="hljs-keyword">public</span> <span class="hljs-function"><span class="hljs-keyword">function</span> <span class="hljs-title">testIsTheContrastEqual</span><span class="hljs-params">()</span></span><span class="hljs-function">    </span>&#123;        <span class="hljs-keyword">$this</span>-&gt;assertEquals(<span class="hljs-string">'hello'</span>, (<span class="hljs-keyword">new</span> test)-&gt;hello());    &#125;    <span class="hljs-keyword">public</span> <span class="hljs-function"><span class="hljs-keyword">function</span> <span class="hljs-title">testIsThereAnyAttribute</span><span class="hljs-params">()</span></span><span class="hljs-function">    </span>&#123;        <span class="hljs-keyword">$this</span>-&gt;assertClassHasAttribute(<span class="hljs-string">'attr'</span>, test::class);    &#125;&#125;</code></pre></li><li>执行结果<br><img src="https://cdn.jsdelivr.net/gh/loquy/loquy.github.io/images/PHPUnit.jpg" srcset="/img/loading.gif" alt="execution"></li></ul>]]></content>
    
    
    <categories>
      
      <category>PHP</category>
      
    </categories>
    
    
    <tags>
      
      <tag>单元测试</tag>
      
      <tag>PHPUnit</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>在 PHP 中使用多进程</title>
    <link href="/68d862bb.html"/>
    <url>/68d862bb.html</url>
    
    <content type="html"><![CDATA[<h2 id="多道程序设计以及顺序和并发执行"><a href="#多道程序设计以及顺序和并发执行" class="headerlink" title="多道程序设计以及顺序和并发执行"></a>多道程序设计以及顺序和并发执行</h2><ul><li>所谓多道程序设计，就是允许多个程序同时进入内存并运行。多道程序设计是操作系统所采用的最基本、最重要的技术，其根本目的是提高整个系统的效率。</li><li>程序是一个在时间上按严格次序前后相继的操作序列，具有独立功能的程序独占处理器直到得到最终结果的过程称为程序的顺序执行。所谓程序的并发执行，是指两个或两个以上程序在计算机系统中，同时处于开始执行且尚未结束的状态。</li></ul><h3 id="进程和线程"><a href="#进程和线程" class="headerlink" title="进程和线程"></a>进程和线程</h3><ul><li>进程是具有一定独立功能的程序在某个数据集合上的一次运行活动，是系统进行资源分配和调度的一个基本单位。</li><li>线程是进程中的一个实体，是处理器调度和分派的基本单位。线程自己基本上不拥有系统资源，只拥有少量在运行中必不可少的资源（如程序计数器、一组寄存器和栈等），但它可与同属一个进程的其他线程共享进程所拥有的全部资源。一个线程可以创建和撤销另一个线程；同一个进程中的多个线程之间可以并发执行。由于线程之间的制约，至使线程在运行中也呈现出间断性。相应的，线程也同样拥有就绪、等待和运行三种基本状态。有的系统中线程还有终止状态等。</li></ul><h2 id="相关函数"><a href="#相关函数" class="headerlink" title="相关函数"></a>相关函数</h2><h3 id="程序执行函数"><a href="#程序执行函数" class="headerlink" title="程序执行函数"></a>程序执行函数</h3><ul><li><a href="https://www.php.net/manual/zh/function.escapeshellarg.php" target="_blank" rel="noopener">escapeshellarg</a> — 把字符串转码为可以在 shell 命令里使用的参数</li><li><a href="https://www.php.net/manual/zh/function.escapeshellcmd.php" target="_blank" rel="noopener">escapeshellcmd</a> — shell 元字符转义</li><li><a href="https://www.php.net/manual/zh/function.exec.php" target="_blank" rel="noopener">exec</a> — 执行一个外部程序</li><li><a href="https://www.php.net/manual/zh/function.passthru.php" target="_blank" rel="noopener">passthru</a> — 执行外部程序并且显示原始输出</li><li><a href="https://www.php.net/manual/zh/function.proc-close.php" target="_blank" rel="noopener">proc_close</a> — 关闭由 proc_open 打开的进程并且返回进程退出码</li><li><a href="https://www.php.net/manual/zh/function.proc-get-status.php" target="_blank" rel="noopener">proc_get_status</a> — 获取由 proc_open 函数打开的进程的信息</li><li><a href="https://www.php.net/manual/zh/function.proc-nice.php" target="_blank" rel="noopener">proc_nice</a> — 修改当前进程的优先级</li><li><a href="https://www.php.net/manual/zh/function.proc-open.php" target="_blank" rel="noopener">proc_open</a> — 执行一个命令，并且打开用来输入/输出的文件指针。</li><li><a href="https://www.php.net/manual/zh/function.proc-terminate.php" target="_blank" rel="noopener">proc_terminate</a> — 杀除由 proc_open 打开的进程</li><li><a href="https://www.php.net/manual/zh/function.shell-exec.php" target="_blank" rel="noopener">shell_exec</a> — 通过 shell 环境执行命令，并且将完整的输出以字符串的方式返回。</li><li><a href="https://www.php.net/manual/zh/function.system.php" target="_blank" rel="noopener">system</a> — 执行外部程序，并且显示输出</li></ul><h3 id="文件系统函数"><a href="#文件系统函数" class="headerlink" title="文件系统函数"></a>文件系统函数</h3><ul><li><a href="https://www.php.net/manual/zh/function.pclose.php" target="_blank" rel="noopener">pclose</a> — 关闭进程文件指针</li><li><a href="https://www.php.net/manual/zh/function.popen.php" target="_blank" rel="noopener">popen</a> — 打开进程文件指针</li></ul><h3 id="网络函数"><a href="#网络函数" class="headerlink" title="网络函数"></a>网络函数</h3><ul><li><a href="https://www.php.net/manual/zh/function.fsockopen.php" target="_blank" rel="noopener">fsockopen</a> — 打开一个网络连接或者一个Unix套接字连接</li></ul><h2 id="代码示例"><a href="#代码示例" class="headerlink" title="代码示例"></a>代码示例</h2><h3 id="开启多个进程，检查代理节点-ip-port-是否可以连接"><a href="#开启多个进程，检查代理节点-ip-port-是否可以连接" class="headerlink" title="开启多个进程，检查代理节点 ip:port 是否可以连接"></a>开启多个进程，检查代理节点 ip:port 是否可以连接</h3><p>deadoralive.php</p><pre><code class="hljs php"><span class="hljs-meta">&lt;?php</span>$json = file_get_contents(<span class="hljs-string">'./node.json'</span>);$array = json_decode($json, <span class="hljs-keyword">true</span>);$array = array_chunk($array, $argv[<span class="hljs-number">1</span>]);$connection = [];$success = <span class="hljs-number">0</span>;$unit = $argv[<span class="hljs-number">2</span>];<span class="hljs-keyword">foreach</span> ($array[$unit] <span class="hljs-keyword">as</span> $value) &#123;    $ip = $value[<span class="hljs-string">'server'</span>];    $port = $value[<span class="hljs-string">'server_port'</span>];    $deadoralive = @fsockopen($ip, $port, $errno, $errstr, <span class="hljs-number">1</span>);    <span class="hljs-keyword">if</span> (!$deadoralive) &#123;        <span class="hljs-keyword">echo</span> getmypid() . <span class="hljs-string">" | The IP address, $ip, is dead"</span> . <span class="hljs-string">"\n"</span>;    &#125; <span class="hljs-keyword">else</span> &#123;        $success++;        $connection[] = $value;        <span class="hljs-keyword">echo</span> getmypid() . <span class="hljs-string">" | The IP address, $ip, is alive"</span>. <span class="hljs-string">"\n"</span>;    &#125;&#125;$total = count($array[$unit]);<span class="hljs-keyword">echo</span> <span class="hljs-string">"\033[31m unit: $unit |total: $total |success: $success \033[0m \n"</span>;$connection = print_r($connection, <span class="hljs-keyword">true</span>);file_put_contents(<span class="hljs-string">'./alivenode_'</span> . $argv[<span class="hljs-number">3</span>] . <span class="hljs-string">'.log'</span>, $connection, FILE_APPEND);</code></pre><p>process.php</p><pre><code class="hljs php"><span class="hljs-meta">&lt;?php</span>$time = time();$begin = round(microtime(<span class="hljs-keyword">true</span>), <span class="hljs-number">4</span>);$json = file_get_contents(<span class="hljs-string">'./node.json'</span>);$array = json_decode($json, <span class="hljs-keyword">true</span>);$size = <span class="hljs-number">5</span>;$chunk = count($array) / $size;$chunk = ceil($chunk);<span class="hljs-comment">// 此处使用 popen 开启多进程，同理可以使用 proc_open、exec 等函数实现</span><span class="hljs-keyword">for</span> ($i=<span class="hljs-number">0</span>; $i &lt; $chunk; $i++) &#123;    $pipe[$i] = popen(<span class="hljs-string">'php ./deadoralive.php '</span> . $size . <span class="hljs-string">' '</span>. $i . <span class="hljs-string">' '</span> . $time, <span class="hljs-string">'w'</span>);&#125;<span class="hljs-keyword">for</span> ($i=<span class="hljs-number">0</span>; $i &lt; $chunk; $i++) &#123;    $r = pclose($pipe[$i]);&#125;$end = round(microtime(<span class="hljs-keyword">true</span>), <span class="hljs-number">4</span>);<span class="hljs-keyword">echo</span> $end - $begin . <span class="hljs-string">"\n"</span>;</code></pre><p>cli 模式下执行命令 php process.php ，效率还行 181 个节点 12 秒就跑完了<br><img src="https://cdn.jsdelivr.net/gh/loquy/loquy.github.io/images/proxy_checker.jpg" srcset="/img/loading.gif" alt="ouput"></p>]]></content>
    
    
    <categories>
      
      <category>PHP</category>
      
    </categories>
    
    
    <tags>
      
      <tag>PHP</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>Vim 常用命令和配置</title>
    <link href="/3f5e4d56.html"/>
    <url>/3f5e4d56.html</url>
    
    <content type="html"><![CDATA[<h1 id="模式"><a href="#模式" class="headerlink" title="模式"></a>模式</h1><p>在其他模式下使用 <code>Esc</code> 切换回普通模式，命令行模式下使用 <code>:命令</code> + <code>Enter</code> 执行命令。</p><table><thead><tr><th align="left">命令</th><th align="left">模式</th><th align="left">描述</th></tr></thead><tbody><tr><td align="left"><code>默认</code></td><td align="left">普通模式</td><td align="left">控制光标移动，可对文本进行复制、粘贴等操作</td></tr><tr><td align="left"><code>i</code></td><td align="left">插入模式</td><td align="left">写入和编辑文本</td></tr><tr><td align="left"><code>:</code></td><td align="left">命令行模式</td><td align="left">保存或退出文本等，设置 Vim 的配置项</td></tr><tr><td align="left"><code>v</code></td><td align="left">可视模式</td><td align="left">配合方向键选取某一块文本区域，像使用鼠标一样</td></tr></tbody></table><h1 id="命令"><a href="#命令" class="headerlink" title="命令"></a>命令</h1><p>注意 <code>n</code> 为数字，<code>+</code> 表示组合使用，其他的字符对应键盘上的按键。</p><ol><li><p>普通模式：</p><p> 常用的光标移动命令。</p><table><thead><tr><th align="left">命令</th><th align="left">描述</th></tr></thead><tbody><tr><td align="left"><code>n + h</code> 或 <code>n + ←</code></td><td align="left">光标左移 n 个位置</td></tr><tr><td align="left"><code>n + l</code> 或 <code>n + →</code></td><td align="left">光标右移 n 个位置</td></tr><tr><td align="left"><code>n + j</code> 或 <code>n + ↓</code></td><td align="left">光标下移 n 个位置</td></tr><tr><td align="left"><code>n + k</code> 或 <code>n + ↑</code></td><td align="left">光标上移 n 个位置</td></tr><tr><td align="left"><code>ctrl + f</code></td><td align="left">向下滚动一屏</td></tr><tr><td align="left"><code>ctrl + b</code></td><td align="left">向上滚动一屏</td></tr><tr><td align="left"><code>ctrl + d</code></td><td align="left">向下滚动半屏</td></tr><tr><td align="left"><code>ctrl + u</code></td><td align="left">向上滚动半屏</td></tr><tr><td align="left"><code>0</code></td><td align="left">跳到首行，可以理解为无穷大的h</td></tr><tr><td align="left"><code>^</code></td><td align="left">跳到行首开始的第一个非空白字符</td></tr><tr><td align="left"><code>$</code></td><td align="left">跳到行尾，可以理解为无穷大的l</td></tr><tr><td align="left"><code>gg</code> 或 <code>1 + G</code></td><td align="left">跳到首行，可以理解为无穷大的k</td></tr><tr><td align="left"><code>G</code></td><td align="left">跳到末行，可以理解为无穷大的j</td></tr><tr><td align="left"><code>w</code></td><td align="left">跳到下一个词首</td></tr><tr><td align="left"><code>b</code></td><td align="left">跳到上一个词首</td></tr><tr><td align="left"><code>e</code></td><td align="left">跳到下一个词尾</td></tr><tr><td align="left"><code>ge</code></td><td align="left">跳到上一个词尾</td></tr><tr><td align="left"><code>H</code></td><td align="left">移动到屏幕的最上方那一行的第一个字符</td></tr><tr><td align="left"><code>M</code></td><td align="left">移动到屏幕的最中央那一行的第一个字符</td></tr><tr><td align="left"><code>L</code></td><td align="left">移动到屏幕的最下方那一行的第一个字符</td></tr><tr><td align="left"><code>n + Enter</code></td><td align="left">光标向下移动 n 行</td></tr><tr><td align="left"><code>c</code></td><td align="left">删除当前光标后面的部分. 进入插入模式</td></tr><tr><td align="left"><code>cc</code></td><td align="left">将当前行替换为空行，进入插入模式</td></tr><tr><td align="left"><code>cw</code></td><td align="left">删除当前单词的光标右侧部分，进入插入模式</td></tr><tr><td align="left"><code>s</code></td><td align="left">删除当前字母，进入插入模式</td></tr><tr><td align="left"><code>r</code></td><td align="left">替换当前字母，输入一个字母后自动返回普通模式</td></tr><tr><td align="left"><code>a</code></td><td align="left">在光标右侧插入文本</td></tr><tr><td align="left"><code>A</code></td><td align="left">在行末插入文本</td></tr><tr><td align="left"><code>i</code></td><td align="left">在光标左侧插入文本</td></tr><tr><td align="left"><code>I</code></td><td align="left">在行首插入文本</td></tr><tr><td align="left"><code>o</code></td><td align="left">在光标下插入新行</td></tr><tr><td align="left"><code>O</code></td><td align="left">在光标上插入新行</td></tr><tr><td align="left"><code>n</code></td><td align="left">跳转下一个搜索词</td></tr><tr><td align="left"><code>N</code></td><td align="left">跳转上一个搜索词</td></tr></tbody></table><p> 常用的复制、黏贴、删除、撤销、查找等操作。</p><table><thead><tr><th align="left">命令</th><th align="left">描述</th></tr></thead><tbody><tr><td align="left"><code>n + x</code></td><td align="left">剪切（删除）光标右边n个字符</td></tr><tr><td align="left"><code>n + X</code></td><td align="left">剪切（删除）光标左边n个字符</td></tr><tr><td align="left"><code>y</code></td><td align="left">复制在可视模式下选中的文本</td></tr><tr><td align="left"><code>y + n</code></td><td align="left">复制在可视模式下选中的文本</td></tr><tr><td align="left"><code>yy</code></td><td align="left">复制光标所在整行</td></tr><tr><td align="left"><code>n + yy</code></td><td align="left">复制光标所在的向下 n 行</td></tr><tr><td align="left"><code>y + n + w</code></td><td align="left">复制1(n)个词</td></tr><tr><td align="left"><code>y + n + l</code></td><td align="left">复制光标右边1(n)个字符</td></tr><tr><td align="left"><code>y + n + h</code></td><td align="left">复制光标左边1(n)个字符</td></tr><tr><td align="left"><code>y + $</code></td><td align="left">从光标当前位置复制到行尾</td></tr><tr><td align="left"><code>y + 0</code></td><td align="left">从光标当前位置复制到行首</td></tr><tr><td align="left"><code>y + 0</code></td><td align="left">从光标当前位置复制到行首。</td></tr><tr><td align="left"><code>y + 1 + G</code> 或 <code>y + gg</code></td><td align="left">复制光标以上的所有行</td></tr><tr><td align="left"><code>d</code></td><td align="left">剪切（删除）在可视模式下选中的文本</td></tr><tr><td align="left"><code>dd</code></td><td align="left">剪切（删除）光标所在整行</td></tr><tr><td align="left"><code>n + dd</code></td><td align="left">剪切（删除）光标所在的向下 n 行</td></tr><tr><td align="left"><code>d + n + w</code></td><td align="left">剪切（删除）1(n)个词</td></tr><tr><td align="left"><code>d + n + l</code></td><td align="left">剪切（删除）光标右边1(n)个字符</td></tr><tr><td align="left"><code>d + n + h</code></td><td align="left">剪切（删除）光标左边1(n)个字符</td></tr><tr><td align="left"><code>d + $</code></td><td align="left">从光标当前位置剪切（删除）到行尾</td></tr><tr><td align="left"><code>d + 0</code></td><td align="left">从光标当前位置剪切（删除）到行首</td></tr><tr><td align="left"><code>d + 1 + G</code> 或 <code>d + gg</code></td><td align="left">剪切（删除）光标以上的所有行</td></tr><tr><td align="left"><code>d + G</code></td><td align="left">剪切光标以下的所有行</td></tr><tr><td align="left"><code>p</code></td><td align="left">粘贴光标上一行</td></tr><tr><td align="left"><code>P</code></td><td align="left">粘贴到光标下一行</td></tr><tr><td align="left"><code>u</code></td><td align="left">撤销</td></tr><tr><td align="left"><code>ctrl + r</code></td><td align="left">恢复上一步撤销</td></tr></tbody></table></li><li><p>命令行模式：</p><table><thead><tr><th align="left">命令</th><th align="left">描述</th></tr></thead><tbody><tr><td align="left"><code>:w</code></td><td align="left">保存</td></tr><tr><td align="left"><code>:q</code></td><td align="left">退出</td></tr><tr><td align="left"><code>:wq</code> 或 <code>ZZ</code></td><td align="left">保存退出</td></tr><tr><td align="left"><code>:q!</code> 或 <code>ZQ</code></td><td align="left">强制退出（放弃对文档的修改内容）</td></tr><tr><td align="left"><code>:wq!</code></td><td align="left">强制保存退出</td></tr><tr><td align="left"><code>:set nu</code></td><td align="left">显示行号</td></tr><tr><td align="left"><code>:set nonu</code></td><td align="left">不显示行号</td></tr><tr><td align="left"><code>:整数</code></td><td align="left">跳转到该行</td></tr><tr><td align="left"><code>?字符串</code></td><td align="left">在文本中从下至上搜索该字符串</td></tr><tr><td align="left"><code>/字符串</code></td><td align="left">在文本中从上至下搜索该字符串</td></tr><tr><td align="left"><code>:s/one/two</code></td><td align="left">将当前光标所在行的第一个one替换成two</td></tr><tr><td align="left"><code>:s/one/two/g</code></td><td align="left">将当前光标所在行的所有one替换成two</td></tr><tr><td align="left"><code>:%s/one/two/g</code></td><td align="left">将全文中的所有one替换成two</td></tr><tr><td align="left"><code>:m,ny&lt;cr&gt;</code></td><td align="left">复制m行到n行的内容</td></tr><tr><td align="left"><code>:m,nd&lt;cr&gt;</code></td><td align="left">剪切（删除）m行到n行的内容</td></tr><tr><td align="left"><code>:help 命令</code></td><td align="left">查看命令帮助信息</td></tr></tbody></table></li></ol><h1 id="配置"><a href="#配置" class="headerlink" title="配置"></a>配置</h1><ol><li><p>Vim 的全局配置一般在<code>/etc/vim/vimrc或者/etc/vimrc</code>，对所有用户生效。用户个人的配置在<code>~/.vimrc</code>。</p></li><li><p>配置项一般都有”打开”和”关闭”两个设置。”关闭”就是在”打开”前面加上前缀”no”，双引号开始的行表示注释。</p></li><li><p>加上 <code>:</code> 可在命令行模式运行，一些常用配置：</p> <pre><code class="hljs vim"><span class="hljs-comment">" 显示行号</span><span class="hljs-keyword">set</span> <span class="hljs-keyword">number</span><span class="hljs-comment">" 不显示行号</span><span class="hljs-keyword">set</span> nonumber<span class="hljs-comment">" 不与 Vi 兼容（采用 Vim 自己的操作命令）</span><span class="hljs-keyword">set</span> nocompatible<span class="hljs-comment">" 打开语法高亮。自动识别代码，使用多种颜色显示。</span><span class="hljs-keyword">syntax</span> <span class="hljs-keyword">on</span><span class="hljs-comment">" 在底部显示，当前处于普通模式还是插入模式。</span><span class="hljs-keyword">set</span> showmode<span class="hljs-comment">" 普通模式下，在底部显示，当前键入的指令。</span><span class="hljs-keyword">set</span> showcmd<span class="hljs-comment">" 支持使用鼠标。</span><span class="hljs-keyword">set</span> mouse=<span class="hljs-keyword">a</span><span class="hljs-comment">" 使用 utf-8 编码。</span><span class="hljs-keyword">set</span> mouse=<span class="hljs-keyword">a</span><span class="hljs-comment">" 启用256色。</span><span class="hljs-keyword">set</span> t_Co=<span class="hljs-number">256</span><span class="hljs-comment">" 启文件类型检查，并且载入与该类型对应的缩进规则。</span><span class="hljs-keyword">filetype</span> <span class="hljs-built_in">indent</span> <span class="hljs-keyword">on</span><span class="hljs-comment">" 按下回车键后，下一行的缩进会自动跟上一行的缩进保持一致。</span><span class="hljs-keyword">set</span> autoindent<span class="hljs-comment">" Tab 转为多少个空格。</span><span class="hljs-keyword">set</span> softtabstop=<span class="hljs-number">2</span><span class="hljs-comment">" 设置行宽，即一行显示多少个字符。</span><span class="hljs-keyword">set</span> textwidth=<span class="hljs-number">80</span><span class="hljs-comment">" 光标所在的当前行高亮。</span><span class="hljs-keyword">set</span> cursorline<span class="hljs-comment">" 自动折行，即太长的行分成几行显示。</span><span class="hljs-keyword">set</span> wrap<span class="hljs-comment">" 输入搜索模式时，每输入一个字符，就自动跳到第一个匹配的结果。</span><span class="hljs-keyword">set</span> incsearch<span class="hljs-comment">" 搜索时，高亮显示匹配结果。</span><span class="hljs-keyword">set</span> hlsearch<span class="hljs-comment">" 搜索时忽略大小写。</span><span class="hljs-keyword">set</span> ignorecase<span class="hljs-comment">" 递归的按键映射，按 a 和 c 等于按 b。</span>:<span class="hljs-keyword">map</span> <span class="hljs-keyword">a</span> <span class="hljs-keyword">b</span>:<span class="hljs-keyword">map</span> <span class="hljs-keyword">c</span> <span class="hljs-keyword">a</span>    <span class="hljs-comment">" 非递归的按键映射，表示按 d 等于按 a。</span>:<span class="hljs-keyword">noremap</span> d <span class="hljs-keyword">a</span></code></pre></li></ol><h1 id="插件"><a href="#插件" class="headerlink" title="插件"></a>插件</h1><ol><li><p>Unix 安装 <a href="https://github.com/junegunn/vim-plug" target="_blank" rel="noopener">vim-plus</a>（一个极简的Vim插件管理器）：</p><pre><code>curl -fLo ~/.vim/autoload/plug.vim --create-dirs \https://raw.githubusercontent.com/junegunn/vim-plug/master/plug.vim</code></pre></li><li><p>配置：</p><p> 在~/.vimrc中添加要下载的vim插件，然后重启。    </p> <pre><code class="hljs vim"><span class="hljs-comment">" 开始标签</span><span class="hljs-keyword">call</span> plug#begin(<span class="hljs-string">'~/.vim/plugged'</span>)<span class="hljs-comment">" 单引号内些上你需要安装的插件</span>    Plug <span class="hljs-string">'xxx'</span><span class="hljs-comment">" 结束标签</span><span class="hljs-keyword">call</span> plug#end()</code></pre></li><li><p>使用和卸载：</p><p> 在命令行模式输入命令使用插件。</p><table><thead><tr><th align="left">命令</th><th align="left">描述</th></tr></thead><tbody><tr><td align="left"><code>:PlugInstall [name ...] [#threads]</code></td><td align="left">安装插件</td></tr><tr><td align="left"><code>:PlugUpdate [name ...] [#threads]</code></td><td align="left">安装或更新插件</td></tr><tr><td align="left"><code>:PlugClean[!]</code></td><td align="left">删除未列出的插件</td></tr><tr><td align="left"><code>:PlugUpgrade</code></td><td align="left">升级vim-plug</td></tr><tr><td align="left"><code>:PlugStatus</code></td><td align="left">检查插件状态</td></tr><tr><td align="left"><code>:PlugDiff</code></td><td align="left">检查来自先前更新的更改以及未决的更改</td></tr><tr><td align="left"><code>:PlugSnapshot[!] [output path]</code></td><td align="left">生成脚本以还原插件的当前快照</td></tr></tbody></table><p> 更多插件参见 <a href="https://vimawesome.com" target="_blank" rel="noopener">vim Awesome</a>。</p></li></ol><h1 id="参考"><a href="#参考" class="headerlink" title="参考"></a>参考</h1><ul><li><a href="https://www.linuxprobe.com/chapter-04.html" target="_blank" rel="noopener">Vim编辑器与Shell命令脚本</a></li><li><a href="https://www.runoob.com/linux/linux-vim.html" target="_blank" rel="noopener">Linux vi/vim</a></li><li><a href="https://zhuanlan.zhihu.com/p/51440836" target="_blank" rel="noopener">史上最全的Vim命令</a></li><li><a href="http://www.ruanyifeng.com/blog/2018/09/vimrc.html" target="_blank" rel="noopener">Vim 配置入门</a></li><li><a href="https://github.com/junegunn/vim-plug" target="_blank" rel="noopener">vim-plus</a></li><li><a href="https://vimawesome.com" target="_blank" rel="noopener">vim Awesome</a></li><li><a href="https://github.com/vim-china/hello-vim/blob/master/quick-start-guide.md" target="_blank" rel="noopener">Vim 入门教程</a></li></ul>]]></content>
    
    
    <categories>
      
      <category>Linux</category>
      
    </categories>
    
    
    <tags>
      
      <tag>Vim</tag>
      
      <tag>Linux</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>Yii 中活动记录（Active Record）类的使用</title>
    <link href="/6a73a53a.html"/>
    <url>/6a73a53a.html</url>
    
    <content type="html"><![CDATA[<h1 id="Yii中活动记录（Active-Record）类的使用"><a href="#Yii中活动记录（Active-Record）类的使用" class="headerlink" title="Yii中活动记录（Active Record）类的使用"></a>Yii中活动记录（Active Record）类的使用</h1><h2 id="查询数据"><a href="#查询数据" class="headerlink" title="查询数据"></a>查询数据</h2><pre><code class="hljs php"><span class="hljs-comment">/** </span><span class="hljs-comment">定义 Active Record 类后，你可以从相应的数据库表中查询数据。 查询过程大致如下三个步骤：</span><span class="hljs-comment">1.通过 yii\db\ActiveRecord::find() 方法创建一个新的查询生成器对象；</span><span class="hljs-comment">2.使用查询生成器的构建方法来构建你的查询；</span><span class="hljs-comment">3.调用查询生成器的查询方法来取出数据到 Active Record 实例中。</span><span class="hljs-comment">正如你看到的，是不是跟查询生成器的步骤差不多。 唯一有区别的地方在于你用 yii\db\ActiveRecord::find() 去获得一个新的查询生成器对象，这个对象是 yii\db\ActiveQuery， 而不是使用 new 操作符创建一个查询生成器对象。 </span><span class="hljs-comment">下面是一些例子，介绍如何使用 Active Query 查询数据：</span><span class="hljs-comment">**/</span><span class="hljs-comment">// SELECT * FROM `customer` WHERE `id` = 123 </span>$customer = Customer::find()-&gt;where([<span class="hljs-string">'id'</span> =&gt; <span class="hljs-number">123</span>])-&gt;one();$customer = Customer::findOne(<span class="hljs-number">123</span>);$customer = Customer::findOne([<span class="hljs-string">'id'</span> =&gt; <span class="hljs-number">123</span>]);<span class="hljs-comment">// 取回所有活跃客户并以他们的 ID 排序：</span>$customers = Customer::find()    -&gt;where([<span class="hljs-string">'status'</span> =&gt; Customer::STATUS_ACTIVE])    -&gt;orderBy(<span class="hljs-string">'id'</span>)    -&gt;all();<span class="hljs-comment">// 返回 id 是 100, 101, 123, 124 的客户</span><span class="hljs-comment">// SELECT * FROM `customer` WHERE `id` IN (100, 101, 123, 124)</span>$customers = Customer::findAll([<span class="hljs-number">100</span>, <span class="hljs-number">101</span>, <span class="hljs-number">123</span>, <span class="hljs-number">124</span>]);<span class="hljs-comment">// 返回 id 是 123 的活跃客户</span><span class="hljs-comment">// SELECT * FROM `customer` WHERE `id` = 123 AND `status` = 1</span>$customer = Customer::findOne([    <span class="hljs-string">'id'</span> =&gt; <span class="hljs-number">123</span>,    <span class="hljs-string">'status'</span> =&gt; Customer::STATUS_ACTIVE,]);<span class="hljs-comment">// 返回所有不活跃的客户</span><span class="hljs-comment">// SELECT * FROM `customer` WHERE `status` = 0</span>$customers = Customer::findAll([    <span class="hljs-string">'status'</span> =&gt; Customer::STATUS_INACTIVE,]);<span class="hljs-comment">// 取回活跃客户的数量：</span><span class="hljs-comment">// SELECT COUNT(*) FROM `customer` WHERE `status` = 1</span>$count = Customer::find()    -&gt;where([<span class="hljs-string">'status'</span> =&gt; Customer::STATUS_ACTIVE])    -&gt;count();<span class="hljs-comment">// 以客户 ID 索引结果集：</span><span class="hljs-comment">// SELECT * FROM `customer`</span>$customers = Customer::find()    -&gt;indexBy(<span class="hljs-string">'id'</span>)    -&gt;all();<span class="hljs-comment">// 警告： 如果你需要将用户输入传递给这些方法，请确保输入值是标量或者是 数组条件，确保数组结构不能被外部所改变：</span><span class="hljs-comment">// yii\web\Controller 确保了 $id 是标量</span><span class="hljs-keyword">public</span> <span class="hljs-function"><span class="hljs-keyword">function</span> <span class="hljs-title">actionView</span><span class="hljs-params">($id)</span></span><span class="hljs-function"></span>&#123;    $model = Post::findOne($id);    <span class="hljs-comment">// ...</span>&#125;<span class="hljs-comment">// 明确了指定要搜索的列，在此处传递标量或数组将始终只是查找出单个记录而已</span>$model = Post::findOne([<span class="hljs-string">'id'</span> =&gt; Yii::$app-&gt;request-&gt;get(<span class="hljs-string">'id'</span>)]);<span class="hljs-comment">// 不要使用下面的代码！可以注入一个数组条件来匹配任意列的值！</span>$model = Post::findOne(Yii::$app-&gt;request-&gt;get(<span class="hljs-string">'id'</span>));</code></pre><h2 id="数据转换"><a href="#数据转换" class="headerlink" title="数据转换"></a>数据转换</h2><pre><code class="hljs php"><span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">Customer</span> <span class="hljs-keyword">extends</span> <span class="hljs-title">ActiveRecord</span></span><span class="hljs-class"></span>&#123;    <span class="hljs-comment">// ...</span>    <span class="hljs-keyword">public</span> <span class="hljs-function"><span class="hljs-keyword">function</span> <span class="hljs-title">getBirthdayText</span><span class="hljs-params">()</span></span><span class="hljs-function">    </span>&#123;        <span class="hljs-keyword">return</span> date(<span class="hljs-string">'Y/m/d'</span>, <span class="hljs-keyword">$this</span>-&gt;birthday);    &#125;        <span class="hljs-keyword">public</span> <span class="hljs-function"><span class="hljs-keyword">function</span> <span class="hljs-title">setBirthdayText</span><span class="hljs-params">($value)</span></span><span class="hljs-function">    </span>&#123;        <span class="hljs-keyword">$this</span>-&gt;birthday = strtotime($value);    &#125;&#125;<span class="hljs-comment">// 访问$customer-&gt;birthdayText</span></code></pre><h2 id="以数组形式获取数据"><a href="#以数组形式获取数据" class="headerlink" title="以数组形式获取数据"></a>以数组形式获取数据</h2><pre><code class="hljs php"><span class="hljs-comment">// 返回所有客户</span><span class="hljs-comment">// 每个客户返回一个关联数组</span>$customers = Customer::find()    -&gt;asArray()    -&gt;all();<span class="hljs-comment">// 提示： 虽然这种方法可以节省内存并提高性能，但它更靠近较低的 DB 抽象层 你将失去大部分的 Active Record 提供的功能。 一个非常重要的区别在于列值的数据类型。 当您在 Active Record 实例中返回数据时，列值将根据实际列类型，自动类型转换； 然而，当您以数组返回数据时，列值将为 字符串（因为它们是没有处理过的 PDO 的结果），不管它们的实际列是什么类型。</span></code></pre><h2 id="批量获取数据"><a href="#批量获取数据" class="headerlink" title="批量获取数据"></a>批量获取数据</h2><pre><code class="hljs php"><span class="hljs-comment">// 每次获取 10 条客户数据</span><span class="hljs-keyword">foreach</span> (Customer::find()-&gt;batch(<span class="hljs-number">10</span>) <span class="hljs-keyword">as</span> $customers) &#123;    <span class="hljs-comment">// $customers 是个最多拥有 10 条数据的数组</span>&#125;<span class="hljs-comment">// 每次获取 10 条客户数据，然后一条一条迭代它们</span><span class="hljs-keyword">foreach</span> (Customer::find()-&gt;each(<span class="hljs-number">10</span>) <span class="hljs-keyword">as</span> $customer) &#123;    <span class="hljs-comment">// $customer 是个 `Customer` 对象</span>&#125;<span class="hljs-comment">// 贪婪加载模式的批处理查询</span><span class="hljs-keyword">foreach</span> (Customer::find()-&gt;with(<span class="hljs-string">'orders'</span>)-&gt;each() <span class="hljs-keyword">as</span> $customer) &#123;    <span class="hljs-comment">// $customer 是个 `Customer` 对象，并附带关联的 `'orders'`</span>&#125;</code></pre><h2 id="保存数据"><a href="#保存数据" class="headerlink" title="保存数据"></a>保存数据</h2><pre><code class="hljs php"><span class="hljs-comment">// 插入新记录</span>$customer = <span class="hljs-keyword">new</span> Customer();$customer-&gt;name = <span class="hljs-string">'James'</span>;$customer-&gt;email = <span class="hljs-string">'james@example.com'</span>;$customer-&gt;save();<span class="hljs-comment">// 更新已存在的记录</span>$customer = Customer::findOne(<span class="hljs-number">123</span>);$customer-&gt;email = <span class="hljs-string">'james@newexample.com'</span>;$customer-&gt;save();<span class="hljs-comment">// save() 方法可能插入或者更新表的记录，这取决于 Active Record 实例的状态。 如果实例通过 new 操作符实例化，调用 save() 方法将插入新记录； 如果实例是一个查询方法的结果，调用 save() 方法 将更新这个实例对应的表记录行。</span></code></pre><h2 id="数据验证"><a href="#数据验证" class="headerlink" title="数据验证"></a>数据验证</h2><pre><code>因为 yii\db\ActiveRecord 继承于 yii\base\Model，它共享相同的 输入验证 功能。 你可以通过重写 rules() 方法声明验证规则并执行， 通过调用 validate() 方法进行数据验证。当你调用 save() 时，默认情况下会自动调用 validate()。 只有当验证通过时，它才会  真正地保存数据; 否则将简单地返回 false， 您可以检查 errors 属性来获取验证过程的错误消息。提示： 如果你确定你的数据不需要验证（比如说数据来自可信的场景）， 你可以调用 save(false) 来跳过验证过程。</code></pre><h2 id="快赋值"><a href="#快赋值" class="headerlink" title="快赋值"></a>快赋值</h2><pre><code class="hljs php">  <span class="hljs-comment">// 和普通的 模型 一样，你亦可以享受 Active Record 实例的 块赋值 特性。 使用此功能，您可以在单个 PHP 语句中，给 Active Record 实例的多个属性批量赋值， 如下所示。 记住，只有 安全属性 才可以批量赋值。</span>$values = [    <span class="hljs-string">'name'</span> =&gt; <span class="hljs-string">'James'</span>,    <span class="hljs-string">'email'</span> =&gt; <span class="hljs-string">'james@example.com'</span>,];$customer = <span class="hljs-keyword">new</span> Customer();$customer-&gt;attributes = $values;$customer-&gt;save();</code></pre><h2 id="更新计数"><a href="#更新计数" class="headerlink" title="更新计数"></a>更新计数</h2><pre><code class="hljs php"><span class="hljs-comment">// 在数据库表中增加或减少一个字段的值是个常见的任务。我们将这些列称为“计数列”。 您可以使用 updateCounters() 更新一个或多个计数列。 例如，</span>    $post = Post::findOne(<span class="hljs-number">100</span>);<span class="hljs-comment">// UPDATE `post` SET `view_count` = `view_count` + 1 WHERE `id` = 100</span>$post-&gt;updateCounters([<span class="hljs-string">'view_count'</span> =&gt; <span class="hljs-number">1</span>]);<span class="hljs-comment">// 同样，你可以调用 updateAllCounters() 同时更新多条记录的计数列</span><span class="hljs-comment">// UPDATE `customer` SET `age` = `age` + 1</span>Customer::updateAllCounters([<span class="hljs-string">'age'</span> =&gt; <span class="hljs-number">1</span>]);</code></pre><h2 id="更新多个数据行"><a href="#更新多个数据行" class="headerlink" title="更新多个数据行"></a>更新多个数据行</h2><pre><code class="hljs php"><span class="hljs-comment">// UPDATE `customer` SET `status` = 1 WHERE `email` LIKE `%@example.com%`</span>Customer::updateAll([<span class="hljs-string">'status'</span> =&gt; Customer::STATUS_ACTIVE], [<span class="hljs-string">'like'</span>, <span class="hljs-string">'email'</span>, <span class="hljs-string">'@example.com'</span>]);</code></pre><h2 id="删除数据"><a href="#删除数据" class="headerlink" title="删除数据"></a>删除数据</h2><pre><code class="hljs php"><span class="hljs-comment">// 要删除单行数据，首先获取与该行对应的 Active Record 实例，然后调用 yii\db\ActiveRecord::delete() 方法。</span>$customer = Customer::findOne(<span class="hljs-number">123</span>);$customer-&gt;delete();<span class="hljs-comment">// 你可以调用 yii\db\ActiveRecord::deleteAll() 方法删除多行甚至全部的数据。例如,</span>Customer::deleteAll([<span class="hljs-string">'status'</span> =&gt; Customer::STATUS_INACTIVE]);</code></pre><h2 id="事务操作"><a href="#事务操作" class="headerlink" title="事务操作"></a>事务操作</h2><pre><code class="hljs php">$customer = Customer::findOne(<span class="hljs-number">123</span>);Customer::getDb()-&gt;transaction(<span class="hljs-function"><span class="hljs-keyword">function</span><span class="hljs-params">($db)</span> <span class="hljs-title">use</span> <span class="hljs-params">($customer)</span> </span>&#123;    $customer-&gt;id = <span class="hljs-number">200</span>;    $customer-&gt;save();    <span class="hljs-comment">// ...其他 DB 操作...</span>&#125;);    <span class="hljs-comment">// 或者</span>$transaction = Customer::getDb()-&gt;beginTransaction();<span class="hljs-keyword">try</span> &#123;    $customer-&gt;id = <span class="hljs-number">200</span>;    $customer-&gt;save();    <span class="hljs-comment">// ...other DB operations...</span>    $transaction-&gt;commit();&#125; <span class="hljs-keyword">catch</span>(\<span class="hljs-keyword">Exception</span> $e) &#123;    $transaction-&gt;rollBack();    <span class="hljs-keyword">throw</span> $e;&#125; <span class="hljs-keyword">catch</span>(\Throwable $e) &#123;    $transaction-&gt;rollBack();    <span class="hljs-keyword">throw</span> $e;&#125;        <span class="hljs-comment">// 提示： 在上面的代码中，我们有两个catch块用于兼容 PHP 5.x 和 PHP 7.x。 \Exception 继承于 \Throwable interface 由于 PHP 7.0 的改动，如果您的应用程序仅使用 PHP 7.0 及更高版本，您可以跳过 \Exception 部分。</span></code></pre><h2 id="乐观锁"><a href="#乐观锁" class="headerlink" title="乐观锁"></a>乐观锁</h2><pre><code>乐观锁是一种防止此冲突的方法：一行数据 同时被多个用户更新。例如，同一时间内，用户 A 和用户 B 都在编辑 相同的 wiki 文章。用户 A 保存他的编辑后，用户 B 也点击“保存”按钮来 保存他的编辑。实际上，用户 B 正在处理的是过时版本的文章， 因此最好是，想办法阻止他保存文章并向他提示一些信息。乐观锁通过使用一个字段来记录每行的版本号来解决上述问题。 当使用过时的版本号保存一行数据时，yii\db\StaleObjectException 异常 将被抛出，这阻止了该行的保存。乐观锁只支持更新 yii\db\ActiveRecord::update() 或者删除 yii\db\ActiveRecord::delete() 已经存在的单条数据行。使用乐观锁的步骤，1.在与 Active Record 类相关联的 DB 表中创建一个列，以存储每行的版本号。 这个列应当是长整型（在 MySQL 中是 BIGINT DEFAULT 0）。2.重写 yii\db\ActiveRecord::optimisticLock() 方法返回这个列的命名。3.在你的 Model 类里实现 OptimisticLockBehavior 行为（注：这个行为类在 2.0.16 版本加入），以便从请求参数里自动解析这个列的值。 然后从验证规则中删除 version 属性，因为 OptimisticLockBehavior 已经处理它了.4.在用于用户填写的 Web 表单中，添加一个隐藏字段（hidden field）来存储正在更新的行的当前版本号。5.在使用 Active Record 更新数据的控制器动作中，要捕获（try/catch） yii\db\StaleObjectException 异常。 实现一些业务逻辑来解决冲突（例如合并更改，提示陈旧的数据等等）。例如，假定版本列被命名为 version。您可以使用下面的代码来实现乐观锁。</code></pre><pre><code class="hljs php"><span class="hljs-comment">// ------ 视图层代码 -------</span><span class="hljs-keyword">use</span> <span class="hljs-title">yii</span>\<span class="hljs-title">helpers</span>\<span class="hljs-title">Html</span>;<span class="hljs-comment">// ...其他输入栏</span><span class="hljs-keyword">echo</span> Html::activeHiddenInput($model, <span class="hljs-string">'version'</span>);<span class="hljs-comment">// ------ 控制器代码 -------</span><span class="hljs-keyword">use</span> <span class="hljs-title">yii</span>\<span class="hljs-title">db</span>\<span class="hljs-title">StaleObjectException</span>;<span class="hljs-keyword">public</span> <span class="hljs-function"><span class="hljs-keyword">function</span> <span class="hljs-title">actionUpdate</span><span class="hljs-params">($id)</span></span><span class="hljs-function"></span>&#123;    $model = <span class="hljs-keyword">$this</span>-&gt;findModel($id);    <span class="hljs-keyword">try</span> &#123;        <span class="hljs-keyword">if</span> ($model-&gt;load(Yii::$app-&gt;request-&gt;post()) &amp;&amp; $model-&gt;save()) &#123;            <span class="hljs-keyword">return</span> <span class="hljs-keyword">$this</span>-&gt;redirect([<span class="hljs-string">'view'</span>, <span class="hljs-string">'id'</span> =&gt; $model-&gt;id]);        &#125; <span class="hljs-keyword">else</span> &#123;            <span class="hljs-keyword">return</span> <span class="hljs-keyword">$this</span>-&gt;render(<span class="hljs-string">'update'</span>, [                <span class="hljs-string">'model'</span> =&gt; $model,            ]);        &#125;    &#125; <span class="hljs-keyword">catch</span> (StaleObjectException $e) &#123;        <span class="hljs-comment">// 解决冲突的代码</span>    &#125;&#125;<span class="hljs-comment">// ------ Model 代码 -------</span><span class="hljs-keyword">use</span> <span class="hljs-title">yii</span>\<span class="hljs-title">behaviors</span>\<span class="hljs-title">OptimisticLockBehavior</span>;<span class="hljs-keyword">public</span> <span class="hljs-function"><span class="hljs-keyword">function</span> <span class="hljs-title">behaviors</span><span class="hljs-params">()</span></span><span class="hljs-function"></span>&#123;    <span class="hljs-keyword">return</span> [        OptimisticLockBehavior::className(),    ];&#125;</code></pre><h2 id="使用关联数据"><a href="#使用关联数据" class="headerlink" title="使用关联数据"></a>使用关联数据</h2><pre><code class="hljs php"><span class="hljs-comment">// 声明关联关系</span><span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">Customer</span> <span class="hljs-keyword">extends</span> <span class="hljs-title">ActiveRecord</span></span><span class="hljs-class"></span>&#123;    <span class="hljs-comment">// ...</span>    <span class="hljs-keyword">public</span> <span class="hljs-function"><span class="hljs-keyword">function</span> <span class="hljs-title">getOrders</span><span class="hljs-params">()</span></span><span class="hljs-function">    </span>&#123;        <span class="hljs-keyword">return</span> <span class="hljs-keyword">$this</span>-&gt;hasMany(Order::className(), [<span class="hljs-string">'customer_id'</span> =&gt; <span class="hljs-string">'id'</span>]);    &#125;&#125;<span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">Order</span> <span class="hljs-keyword">extends</span> <span class="hljs-title">ActiveRecord</span></span><span class="hljs-class"></span>&#123;    <span class="hljs-comment">// ...</span>    <span class="hljs-keyword">public</span> <span class="hljs-function"><span class="hljs-keyword">function</span> <span class="hljs-title">getCustomer</span><span class="hljs-params">()</span></span><span class="hljs-function">    </span>&#123;        <span class="hljs-keyword">return</span> <span class="hljs-keyword">$this</span>-&gt;hasOne(Customer::className(), [<span class="hljs-string">'id'</span> =&gt; <span class="hljs-string">'customer_id'</span>]);    &#125;&#125;<span class="hljs-comment">/** 上述的代码中，我们为 Customer 类声明了一个 orders 关联， 和为 Order 声明了一个 customer 关联。</span><span class="hljs-comment"></span><span class="hljs-comment"> 每个关联方法必须这样命名：getXyz。然后我们通过 xyz（首字母小写）调用这个关联名。 请注意关联名是大小写敏感的。</span><span class="hljs-comment"></span><span class="hljs-comment">当声明一个关联关系的时候，必须指定好以下的信息：</span><span class="hljs-comment"></span><span class="hljs-comment">关联的对应关系：通过调用 hasMany() 或者 hasOne() 指定。在上面的例子中，您可以很容易看出这样的关联声明： 一个客户可以有很多订单，而每个订单只有一个客户。</span><span class="hljs-comment">相关联 Active Record 类名：用来指定为 hasMany() 或者 hasOne() 方法的第一个参数。 推荐的做法是调用 Xyz::className() 来获取类名称的字符串，以便您 可以使用 IDE 的自动补全，以及让编译阶段的错误检测生效。</span><span class="hljs-comment">两组数据的关联列：用以指定两组数据相关的列（hasOne()/hasMany() 的第二个参数）。 数组的值填的是主数据的列（当前要声明关联的 Active Record 类为主数据）， 而数组的键要填的是相关数据的列。</span><span class="hljs-comment"></span><span class="hljs-comment">一个简单的口诀，先附表的主键，后主表的主键。 正如上面的例子，customer_id 是 Order 的属性，而 id是 Customer 的属性。 （译者注：hasMany() 的第二个参数，这个数组键值顺序不要弄反了）**/</span>    <span class="hljs-comment">// 访问关联数据</span>    <span class="hljs-comment">// SELECT * FROM `customer` WHERE `id` = 123</span>$customer = Customer::findOne(<span class="hljs-number">123</span>);<span class="hljs-comment">// SELECT * FROM `order` WHERE `customer_id` = 123</span><span class="hljs-comment">// $orders 是由 Order 类组成的数组</span>$orders = $customer-&gt;orders;<span class="hljs-comment">//  当你通过 getter 方法 getXyz() 声明了一个叫 xyz 的关联属性，你就可以像 属性 那样访问 xyz。注意这个命名是区分大小写的。</span><span class="hljs-comment">// 提示： 虽然这个概念跟 这个 属性 特性很像， 但是还是有一个很重要的区别。普通对象属性的属性值与其定义的 getter 方法的类型是相同的。 而关联方法返回的是一个 yii\db\ActiveQuery 活动查询生成器的实例。只有当访问关联属性的的时候， 才会返回 yii\db\ActiveRecord Active Record 实例，或者 Active Record 实例组成的数组。</span>$customer-&gt;orders; <span class="hljs-comment">// 获得 `Order` 对象的数组</span>$customer-&gt;getOrders(); <span class="hljs-comment">// 返回 ActiveQuery 类的实例</span></code></pre><h2 id="动态关联查询"><a href="#动态关联查询" class="headerlink" title="动态关联查询"></a>动态关联查询</h2><pre><code class="hljs php">$customer = Customer::findOne(<span class="hljs-number">123</span>);<span class="hljs-comment">// SELECT * FROM `order` WHERE `customer_id` = 123 AND `subtotal` &gt; 200 ORDER BY `id`</span>$orders = $customer-&gt;getOrders()    -&gt;where([<span class="hljs-string">'&gt;'</span>, <span class="hljs-string">'subtotal'</span>, <span class="hljs-number">200</span>])    -&gt;orderBy(<span class="hljs-string">'id'</span>)    -&gt;all();<span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">Customer</span> <span class="hljs-keyword">extends</span> <span class="hljs-title">ActiveRecord</span></span><span class="hljs-class"></span>&#123;    <span class="hljs-keyword">public</span> <span class="hljs-function"><span class="hljs-keyword">function</span> <span class="hljs-title">getBigOrders</span><span class="hljs-params">($threshold = <span class="hljs-number">100</span>)</span> // 老司机的提醒机的提醒<span class="hljs-title">threshold</span> 参数一定一定要给个默认值一定要给个默认值</span>&#123;        <span class="hljs-keyword">return</span> <span class="hljs-keyword">$this</span>-&gt;hasMany(Order::className(), [<span class="hljs-string">'customer_id'</span> =&gt; <span class="hljs-string">'id'</span>])            -&gt;where(<span class="hljs-string">'subtotal &gt; :threshold'</span>, [<span class="hljs-string">':threshold'</span> =&gt; $threshold])            -&gt;orderBy(<span class="hljs-string">'id'</span>);    &#125;&#125;<span class="hljs-comment">// 然后你就可以执行以下关联查询：</span><span class="hljs-comment">// SELECT * FROM `order` WHERE `customer_id` = 123 AND `subtotal` &gt; 200 ORDER BY `id`</span>$orders = $customer-&gt;getBigOrders(<span class="hljs-number">200</span>)-&gt;all();<span class="hljs-comment">// SELECT * FROM `order` WHERE `customer_id` = 123 AND `subtotal` &gt; 100 ORDER BY `id`</span>$orders = $customer-&gt;bigOrders;</code></pre><h2 id="中间表关联"><a href="#中间表关联" class="headerlink" title="中间表关联"></a>中间表关联</h2><pre><code class="hljs php"><span class="hljs-comment">// 在数据库建模中，当两个关联表之间的对应关系是多对多时， 通常会引入一个连接表。例如，order 表 和 item 表可以通过名为 order_item 的连接表相关联。一个 order 将关联多个 order items， 而一个 order item 也会关联到多个 orders。</span><span class="hljs-comment">// 当声明这种表关联后，您可以调用 via() 或 viaTable() 指明连接表。via() 和 viaTable() 之间的区别是 前者是根据现有的关联名称来指定连接表，而后者直接使用 连接表。例如，</span><span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">Order</span> <span class="hljs-keyword">extends</span> <span class="hljs-title">ActiveRecord</span></span><span class="hljs-class"></span>&#123;    <span class="hljs-keyword">public</span> <span class="hljs-function"><span class="hljs-keyword">function</span> <span class="hljs-title">getItems</span><span class="hljs-params">()</span></span><span class="hljs-function">    </span>&#123;        <span class="hljs-keyword">return</span> <span class="hljs-keyword">$this</span>-&gt;hasMany(Item::className(), [<span class="hljs-string">'id'</span> =&gt; <span class="hljs-string">'item_id'</span>])            -&gt;viaTable(<span class="hljs-string">'order_item'</span>, [<span class="hljs-string">'order_id'</span> =&gt; <span class="hljs-string">'id'</span>]);    &#125;&#125;<span class="hljs-comment">// 或者,</span><span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">Order</span> <span class="hljs-keyword">extends</span> <span class="hljs-title">ActiveRecord</span></span><span class="hljs-class"></span>&#123;    <span class="hljs-keyword">public</span> <span class="hljs-function"><span class="hljs-keyword">function</span> <span class="hljs-title">getOrderItems</span><span class="hljs-params">()</span></span><span class="hljs-function">    </span>&#123;        <span class="hljs-keyword">return</span> <span class="hljs-keyword">$this</span>-&gt;hasMany(OrderItem::className(), [<span class="hljs-string">'order_id'</span> =&gt; <span class="hljs-string">'id'</span>]);    &#125;    <span class="hljs-keyword">public</span> <span class="hljs-function"><span class="hljs-keyword">function</span> <span class="hljs-title">getItems</span><span class="hljs-params">()</span></span><span class="hljs-function">    </span>&#123;        <span class="hljs-keyword">return</span> <span class="hljs-keyword">$this</span>-&gt;hasMany(Item::className(), [<span class="hljs-string">'id'</span> =&gt; <span class="hljs-string">'item_id'</span>])            -&gt;via(<span class="hljs-string">'orderItems'</span>);    &#125;&#125;<span class="hljs-comment">// 使用连接表声明的关联和正常声明的关联是等同的，例如，</span><span class="hljs-comment">// SELECT * FROM `order` WHERE `id` = 100</span>$order = Order::findOne(<span class="hljs-number">100</span>);<span class="hljs-comment">// SELECT * FROM `order_item` WHERE `order_id` = 100</span><span class="hljs-comment">// SELECT * FROM `item` WHERE `item_id` IN (...)</span><span class="hljs-comment">// 返回 Item 类组成的数组</span>$items = $order-&gt;items;</code></pre><h2 id="通过多个表来连接关联声明"><a href="#通过多个表来连接关联声明" class="headerlink" title="通过多个表来连接关联声明"></a>通过多个表来连接关联声明</h2><pre><code class="hljs php"><span class="hljs-comment">// 通过使用 via() 方法，它还可以通过多个表来定义关联声明。 再考虑考虑上面的例子，我们有 Customer，Order 和 Item 类。 我们可以添加一个关联关系到 Customer 类，这个关联可以列出了 Customer（客户） 的订单下放置的所有 Item（商品）， 这个关联命名为 getPurchasedItems()，关联声明如下代码示例所示：</span><span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">Customer</span> <span class="hljs-keyword">extends</span> <span class="hljs-title">ActiveRecord</span></span><span class="hljs-class"></span>&#123;    <span class="hljs-comment">// ...</span>    <span class="hljs-keyword">public</span> <span class="hljs-function"><span class="hljs-keyword">function</span> <span class="hljs-title">getPurchasedItems</span><span class="hljs-params">()</span></span><span class="hljs-function">    </span>&#123;        <span class="hljs-comment">// 客户的商品，将 Item 中的 'id' 列与 OrderItem 中的 'item_id' 相匹配</span>        <span class="hljs-keyword">return</span> <span class="hljs-keyword">$this</span>-&gt;hasMany(Item::className(), [<span class="hljs-string">'id'</span> =&gt; <span class="hljs-string">'item_id'</span>])                    -&gt;via(<span class="hljs-string">'orderItems'</span>);    &#125;    <span class="hljs-keyword">public</span> <span class="hljs-function"><span class="hljs-keyword">function</span> <span class="hljs-title">getOrderItems</span><span class="hljs-params">()</span></span><span class="hljs-function">    </span>&#123;        <span class="hljs-comment">// 客户订单中的商品，将 `Order` 的 'id' 列和 OrderItem 的 'order_id' 列相匹配</span>        <span class="hljs-keyword">return</span> <span class="hljs-keyword">$this</span>-&gt;hasMany(OrderItem::className(), [<span class="hljs-string">'order_id'</span> =&gt; <span class="hljs-string">'id'</span>])                    -&gt;via(<span class="hljs-string">'orders'</span>);    &#125;    <span class="hljs-keyword">public</span> <span class="hljs-function"><span class="hljs-keyword">function</span> <span class="hljs-title">getOrders</span><span class="hljs-params">()</span></span><span class="hljs-function">    </span>&#123;        <span class="hljs-comment">// 见上述列子</span>        <span class="hljs-keyword">return</span> <span class="hljs-keyword">$this</span>-&gt;hasMany(Order::className(), [<span class="hljs-string">'customer_id'</span> =&gt; <span class="hljs-string">'id'</span>]);    &#125;&#125;</code></pre><h2 id="延迟加载和即时加载"><a href="#延迟加载和即时加载" class="headerlink" title="延迟加载和即时加载"></a>延迟加载和即时加载</h2><pre><code class="hljs php"><span class="hljs-comment">// 在 访问关联数据 中，我们解释说可以像问正常的对象属性那样 访问 Active Record 实例的关联属性。SQL 语句仅在 你第一次访问关联属性时执行。我们称这种关联数据访问方法为 延迟加载。 例如，</span><span class="hljs-comment">// SELECT * FROM `customer` WHERE `id` = 123</span>$customer = Customer::findOne(<span class="hljs-number">123</span>);<span class="hljs-comment">// SELECT * FROM `order` WHERE `customer_id` = 123</span>$orders = $customer-&gt;orders;<span class="hljs-comment">// 没有 SQL 语句被执行</span>$orders2 = $customer-&gt;orders;<span class="hljs-comment">// 延迟加载使用非常方便。但是，当你需要访问相同的具有多个 Active Record 实例的关联属性时， 可能会遇到性能问题。请思考一下以下代码示例。 有多少 SQL 语句会被执行？</span><span class="hljs-comment">// SELECT * FROM `customer` LIMIT 100</span>$customers = Customer::find()-&gt;limit(<span class="hljs-number">100</span>)-&gt;all();<span class="hljs-keyword">foreach</span> ($customers <span class="hljs-keyword">as</span> $customer) &#123;    <span class="hljs-comment">// SELECT * FROM `order` WHERE `customer_id` = ...</span>    $orders = $customer-&gt;orders;&#125;<span class="hljs-comment">// 你瞅瞅，上面的代码会产生 101 次 SQL 查询！ 这是因为每次你访问 for 循环中不同的 Customer 对象的 orders 关联属性时，SQL 语句 都会被执行一次。</span><span class="hljs-comment">// 为了解决上述的性能问题，你可以使用所谓的 即时加载，如下所示，</span><span class="hljs-comment">// SELECT * FROM `customer` LIMIT 100;</span><span class="hljs-comment">// SELECT * FROM `orders` WHERE `customer_id` IN (...)</span>$customers = Customer::find()    -&gt;with(<span class="hljs-string">'orders'</span>)    -&gt;limit(<span class="hljs-number">100</span>)    -&gt;all();<span class="hljs-keyword">foreach</span> ($customers <span class="hljs-keyword">as</span> $customer) &#123;    <span class="hljs-comment">// 没有任何的 SQL 执行</span>    $orders = $customer-&gt;orders;&#125;<span class="hljs-comment">// 通过调用 yii\db\ActiveQuery::with() 方法，你使 Active Record 在一条 SQL 语句里就返回了这 100 位客户的订单。 结果就是，你把要执行的 SQL 语句从 101 减少到 2 条！</span><span class="hljs-comment">// 你可以即时加载一个或多个关联。 你甚至可以即时加载 嵌套关联 。嵌套关联是一种 在相关的 Active Record 类中声明的关联。例如，Customer 通过 orders 关联属性 与 Order 相关联， Order 与 Item 通过 items 关联属性相关联。 当查询 Customer 时，您可以即时加载 通过嵌套关联符 orders.items 关联的 items。</span><span class="hljs-comment">// 以下代码展示了 with() 的各种用法。我们假设 Customer 类 有两个关联 orders 和 country，而 Order 类有一个关联 items。</span><span class="hljs-comment">//  即时加载 "orders" and "country"</span>$customers = Customer::find()-&gt;with(<span class="hljs-string">'orders'</span>, <span class="hljs-string">'country'</span>)-&gt;all();<span class="hljs-comment">// 等同于使用数组语法 如下</span>$customers = Customer::find()-&gt;with([<span class="hljs-string">'orders'</span>, <span class="hljs-string">'country'</span>])-&gt;all();<span class="hljs-comment">// 没有任何的 SQL 执行</span>$orders= $customers[<span class="hljs-number">0</span>]-&gt;orders;<span class="hljs-comment">// 没有任何的 SQL 执行</span>$country = $customers[<span class="hljs-number">0</span>]-&gt;country;<span class="hljs-comment">// 即时加载“订单”和嵌套关系“orders.items”</span>$customers = Customer::find()-&gt;with(<span class="hljs-string">'orders.items'</span>)-&gt;all();<span class="hljs-comment">// 访问第一个客户的第一个订单中的商品</span><span class="hljs-comment">// 没有 SQL 查询执行</span>$items = $customers[<span class="hljs-number">0</span>]-&gt;orders[<span class="hljs-number">0</span>]-&gt;items;<span class="hljs-comment">// 你也可以即时加载更深的嵌套关联，比如 a.b.c.d。所有的父关联都会被即时加载。 那就是, 当你调用 with() 来 with a.b.c.d, 你将即时加载 a, a.b, a.b.c and a.b.c.d。</span><span class="hljs-comment">// 提示： 一般来说，当即时加载 N 个关联，另有 M 个关联 通过 连接表 声明，则会有 N+M+1 条 SQL 语句被执行。 请注意这样的的嵌套关联 a.b.c.d 算四个关联。</span><span class="hljs-comment">// 当即时加载一个关联，你可以通过匿名函数自定义相应的关联查询。 例如，</span><span class="hljs-comment">// 查找所有客户，并带上他们国家和活跃订单</span><span class="hljs-comment">// SELECT * FROM `customer`</span><span class="hljs-comment">// SELECT * FROM `country` WHERE `id` IN (...)</span><span class="hljs-comment">// SELECT * FROM `order` WHERE `customer_id` IN (...) AND `status` = 1</span>$customers = Customer::find()-&gt;with([    <span class="hljs-string">'country'</span>,    <span class="hljs-string">'orders'</span> =&gt; <span class="hljs-function"><span class="hljs-keyword">function</span> <span class="hljs-params">($query)</span> </span>&#123;        $query-&gt;andWhere([<span class="hljs-string">'status'</span> =&gt; Order::STATUS_ACTIVE]);    &#125;,])-&gt;all();<span class="hljs-comment">// 自定义关联查询时，应该将关联名称指定为数组的键 并使用匿名函数作为相应的数组的值。匿名函数将接受一个 $query 参数 它用于表示这个自定义的关联执行关联查询的 yii\db\ActiveQuery 对象。 在上面的代码示例中，我们通过附加一个关于订单状态的附加条件来修改关联查询。</span><span class="hljs-comment">// 提示： 如果你在即时加载的关联中调用 select() 方法，你要确保 在关联声明中引用的列必须被 select。否则，相应的模型（Models）可能 无法加载。例如，</span>$orders = Order::find()-&gt;select([<span class="hljs-string">'id'</span>, <span class="hljs-string">'amount'</span>])-&gt;with(<span class="hljs-string">'customer'</span>)-&gt;all();<span class="hljs-comment">// $orders[0]-&gt;customer 会一直是 `null`。你应该这样写，以解决这个问题：</span>$orders = Order::find()-&gt;select([<span class="hljs-string">'id'</span>, <span class="hljs-string">'amount'</span>, <span class="hljs-string">'customer_id'</span>])-&gt;with(<span class="hljs-string">'cus</span></code></pre><h2 id="关联关系的-JOIN-查询"><a href="#关联关系的-JOIN-查询" class="headerlink" title="关联关系的 JOIN 查询"></a>关联关系的 JOIN 查询</h2><pre><code class="hljs php"><span class="hljs-comment">// 提示： 这小节的内容仅仅适用于关系数据库， 比如 MySQL，PostgreSQL 等等。</span><span class="hljs-comment">// 到目前为止，我们所介绍的关联查询，仅仅是使用主表列 去查询主表数据。实际应用中，我们经常需要在关联表中使用这些列。例如， 我们可能要取出至少有一个活跃订单的客户。为了解决这个问题，我们可以 构建一个 join 查询，如下所示：</span><span class="hljs-comment">// SELECT `customer`.* FROM `customer`</span><span class="hljs-comment">// LEFT JOIN `order` ON `order`.`customer_id` = `customer`.`id`</span><span class="hljs-comment">// WHERE `order`.`status` = 1</span><span class="hljs-comment">// </span><span class="hljs-comment">// SELECT * FROM `order` WHERE `customer_id` IN (...)</span>$customers = Customer::find()    -&gt;select(<span class="hljs-string">'customer.*'</span>)    -&gt;leftJoin(<span class="hljs-string">'order'</span>, <span class="hljs-string">'`order`.`customer_id` = `customer`.`id`'</span>)    -&gt;where([<span class="hljs-string">'order.status'</span> =&gt; Order::STATUS_ACTIVE])    -&gt;with(<span class="hljs-string">'orders'</span>)    -&gt;all();<span class="hljs-comment">// 提示： 在构建涉及 JOIN SQL 语句的连接查询时，清除列名的歧义很重要。 通常的做法是将表名称作为前缀加到对应的列名称前。</span><span class="hljs-comment">// 但是，更好的方法是通过调用 yii\db\ActiveQuery::joinWith() 来利用已存在的关联声明：</span>$customers = Customer::find()    -&gt;joinWith(<span class="hljs-string">'orders'</span>)    -&gt;where([<span class="hljs-string">'order.status'</span> =&gt; Order::STATUS_ACTIVE])    -&gt;all();<span class="hljs-comment">// 两种方法都执行相同的 SQL 语句集。然而，后一种方法更干净、简洁。</span><span class="hljs-comment">// 默认的，joinWith() 会使用 LEFT JOIN 去连接主表和关联表。 你可以通过 $joinType 参数指定不同的连接类型（比如 RIGHT JOIN）。 如果你想要的连接类型是 INNER JOIN，你可以直接用 innerJoinWith() 方法代替。</span><span class="hljs-comment">// 调用 joinWith() 方法会默认 即时加载 相应的关联数据。 如果你不需要那些关联数据，你可以指定它的第二个参数 $eagerLoading 为 false。</span><span class="hljs-comment">// 注意： 即使在启用即时加载的情况下使用 joinWith() 或 innerJoinWith()， 相应的关联数据也不会从这个 JOIN 查询的结果中填充。 因此，每个连接关系还有一个额外的查询，正如即时加载部分所述。</span><span class="hljs-comment">// 和 with() 一样，你可以 join 多个关联表；你可以动态的自定义 你的关联查询；你可以使用嵌套关联进行 join。你也可以将 with() 和 joinWith() 组合起来使用。例如：</span>$customers = Customer::find()-&gt;joinWith([    <span class="hljs-string">'orders'</span> =&gt; <span class="hljs-function"><span class="hljs-keyword">function</span> <span class="hljs-params">($query)</span> </span>&#123;        $query-&gt;andWhere([<span class="hljs-string">'&gt;'</span>, <span class="hljs-string">'subtotal'</span>, <span class="hljs-number">100</span>]);    &#125;,])-&gt;with(<span class="hljs-string">'country'</span>)    -&gt;all();<span class="hljs-comment">// 有时，当连接两个表时，你可能需要在 JOIN 查询的 ON 部分中指定一些额外的条件。 这可以通过调用 yii\db\ActiveQuery::onCondition() 方法来完成，如下所示：</span><span class="hljs-comment">// SELECT `customer`.* FROM `customer`</span><span class="hljs-comment">// LEFT JOIN `order` ON `order`.`customer_id` = `customer`.`id` AND `order`.`status` = 1 </span><span class="hljs-comment">// </span><span class="hljs-comment">// SELECT * FROM `order` WHERE `customer_id` IN (...)</span>$customers = Customer::find()-&gt;joinWith([    <span class="hljs-string">'orders'</span> =&gt; <span class="hljs-function"><span class="hljs-keyword">function</span> <span class="hljs-params">($query)</span> </span>&#123;        $query-&gt;onCondition([<span class="hljs-string">'order.status'</span> =&gt; Order::STATUS_ACTIVE]);    &#125;,])-&gt;all();<span class="hljs-comment">// 以上查询取出 所有 客户，并为每个客户取回所有活跃订单。 请注意，这与我们之前的例子不同，后者仅取出至少有一个活跃订单的客户。</span><span class="hljs-comment">// 提示： 当通过 onCondition() 修改 yii\db\ActiveQuery 时， 如果查询涉及到 JOIN 查询，那么条件将被放在 ON 部分。如果查询不涉及 JOIN ，条件将自动附加到查询的 WHERE 部分。 因此，它可以只包含 包含了关联表的列 的条件。（译者注：意思是 onCondition() 中可以只写关联表的列，主表的列写不写都行）</span></code></pre><h2 id="反向关联"><a href="#反向关联" class="headerlink" title="反向关联"></a>反向关联</h2><pre><code class="hljs php"><span class="hljs-comment">// 两个 Active Record 类之间的关联声明往往是相互关联的。例如，Customer 是 通过 orders 关联到 Order ，而Order 通过 customer 又关联回到了 Customer。</span><span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">Customer</span> <span class="hljs-keyword">extends</span> <span class="hljs-title">ActiveRecord</span></span><span class="hljs-class"></span>&#123;    <span class="hljs-keyword">public</span> <span class="hljs-function"><span class="hljs-keyword">function</span> <span class="hljs-title">getOrders</span><span class="hljs-params">()</span></span><span class="hljs-function">    </span>&#123;        <span class="hljs-keyword">return</span> <span class="hljs-keyword">$this</span>-&gt;hasMany(Order::className(), [<span class="hljs-string">'customer_id'</span> =&gt; <span class="hljs-string">'id'</span>]);    &#125;&#125;<span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">Order</span> <span class="hljs-keyword">extends</span> <span class="hljs-title">ActiveRecord</span></span><span class="hljs-class"></span>&#123;    <span class="hljs-keyword">public</span> <span class="hljs-function"><span class="hljs-keyword">function</span> <span class="hljs-title">getCustomer</span><span class="hljs-params">()</span></span><span class="hljs-function">    </span>&#123;        <span class="hljs-keyword">return</span> <span class="hljs-keyword">$this</span>-&gt;hasOne(Customer::className(), [<span class="hljs-string">'id'</span> =&gt; <span class="hljs-string">'customer_id'</span>]);    &#125;&#125;<span class="hljs-comment">// 现在考虑下面的一段代码：</span><span class="hljs-comment">// SELECT * FROM `customer` WHERE `id` = 123</span>$customer = Customer::findOne(<span class="hljs-number">123</span>);<span class="hljs-comment">// SELECT * FROM `order` WHERE `customer_id` = 123</span>$order = $customer-&gt;orders[<span class="hljs-number">0</span>];<span class="hljs-comment">// SELECT * FROM `customer` WHERE `id` = 123</span>$customer2 = $order-&gt;customer;<span class="hljs-comment">// 显示 "not the same"</span><span class="hljs-keyword">echo</span> $customer2 === $customer ? <span class="hljs-string">'same'</span> : <span class="hljs-string">'not the same'</span>;<span class="hljs-comment">// 我们原本认为 $customer 和 $customer2 是一样的，但不是！其实他们确实包含相同的 客户数据，但它们是不同的对象。 访问 $order-&gt;customer 时，需要执行额外的 SQL 语句， 以填充出一个新对象 $customer2。</span><span class="hljs-comment">// 为了避免上述例子中最后一个 SQL 语句被冗余执行，我们应该告诉 Yii customer 是 orders 的 反向关联，可以通过调用 inverseOf() 方法声明， 如下所示：</span><span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">Customer</span> <span class="hljs-keyword">extends</span> <span class="hljs-title">ActiveRecord</span></span><span class="hljs-class"></span>&#123;    <span class="hljs-keyword">public</span> <span class="hljs-function"><span class="hljs-keyword">function</span> <span class="hljs-title">getOrders</span><span class="hljs-params">()</span></span><span class="hljs-function">    </span>&#123;        <span class="hljs-keyword">return</span> <span class="hljs-keyword">$this</span>-&gt;hasMany(Order::className(), [<span class="hljs-string">'customer_id'</span> =&gt; <span class="hljs-string">'id'</span>])-&gt;inverseOf(<span class="hljs-string">'customer'</span>);    &#125;&#125;<span class="hljs-comment">// 这样修改关联声明后：</span><span class="hljs-comment">// SELECT * FROM `customer` WHERE `id` = 123</span>$customer = Customer::findOne(<span class="hljs-number">123</span>);<span class="hljs-comment">// SELECT * FROM `order` WHERE `customer_id` = 123</span>$order = $customer-&gt;orders[<span class="hljs-number">0</span>];<span class="hljs-comment">// No SQL will be executed</span>$customer2 = $order-&gt;customer;<span class="hljs-comment">// 输出 "same"</span><span class="hljs-keyword">echo</span> $customer2 === $customer ? <span class="hljs-string">'same'</span> : <span class="hljs-string">'not the same'</span>;<span class="hljs-comment">// 注意： 反向关联不能用在有 连接表 关联声明中。 也就是说，如果一个关联关系通过 via() 或 viaTable() 声明， 你就不能再调用 inverseOf() 了。</span></code></pre><h2 id="保存关联数据"><a href="#保存关联数据" class="headerlink" title="保存关联数据"></a>保存关联数据</h2><pre><code class="hljs php"><span class="hljs-comment">// 在使用关联数据时，您经常需要建立不同数据之间的关联或销毁 现有关联。这需要为定义的关联的列设置正确的值。通过使用 Active Record， 你就可以编写如下代码：</span>$customer = Customer::findOne(<span class="hljs-number">123</span>);$order = <span class="hljs-keyword">new</span> Order();$order-&gt;subtotal = <span class="hljs-number">100</span>;<span class="hljs-comment">// ...</span><span class="hljs-comment">// 为 Order 设置属性以定义与 "customer" 的关联关系</span>$order-&gt;customer_id = $customer-&gt;id;$order-&gt;save();Active Record 提供了 link() 方法，可以更好地完成此任务：$customer = Customer::findOne(<span class="hljs-number">123</span>);$order = <span class="hljs-keyword">new</span> Order();$order-&gt;subtotal = <span class="hljs-number">100</span>;<span class="hljs-comment">// ...</span>$order-&gt;link(<span class="hljs-string">'customer'</span>, $customer);<span class="hljs-comment">// link()方法需要指定关联名 和要建立关联的目标 Active Record 实例。该方法将修改属性的值 以连接两个 Active Record 实例，并将其保存到数据库。在上面的例子中，它将设置 Order 实例的 customer_id 属性 为 Customer 实例的 id 属性的值，然后保存 到数据库。</span><span class="hljs-comment">// 注意： 你不能关联两个新的 Active Record 实例。</span><span class="hljs-comment">// 使用 link() 的好处在通过 junction table 定义关系时更加明显。 例如，你可以使用以下代码关联 Order 实例 和 Item 实例：</span>$order-&gt;link(<span class="hljs-string">'items'</span>, $item);<span class="hljs-comment">// 上述代码会自动在 order_item 关联表中插入一行，以关联 order 和 item 这两个数据记录。</span><span class="hljs-comment">// 信息： link() 方法在保存相应的 Active Record 实例时， 将不会执行任何数据验证。在调用此方法之前， 您应当验证所有的输入数据。</span><span class="hljs-comment">//link() 方法的反向操作是 unlink() 方法， 这将可以断掉两个 Active Record 实例间的已经存在了的关联关系。例如，</span>$customer = Customer::find()-&gt;with(<span class="hljs-string">'orders'</span>)-&gt;where([<span class="hljs-string">'id'</span> =&gt; <span class="hljs-number">123</span>])-&gt;one();$customer-&gt;unlink(<span class="hljs-string">'orders'</span>, $customer-&gt;orders[<span class="hljs-number">0</span>]);<span class="hljs-comment">// 默认情况下，unlink() 方法将设置指定的外键值， 以把现有的关联指定为 null。此外，你可以选择通过将 $delete 参数设置为true 传递给方法， 删除包含此外键值的表记录行。</span><span class="hljs-comment">// 当关联关系中有连接表时，调用 unlink() 时， 如果 $delete 参数是 true 的话，将导致 连接表中的外键或相应的行被删除。</span></code></pre><h2 id="跨数据库关联"><a href="#跨数据库关联" class="headerlink" title="跨数据库关联"></a>跨数据库关联</h2><pre><code class="hljs php"><span class="hljs-comment">// Active Record 允许您在不同数据库驱动的 Active Record 类之间声明关联关系。 这些数据库可以是不同的类型（例如 MySQL 和 PostgreSQL ，或是 MS SQL 和 MongoDB），它们也可以运行在 不同的服务器上。你可以使用相同的语法来执行关联查询。例如，</span><span class="hljs-comment">// Customer 对应的表是关系数据库中（比如 MySQL）的 "customer" 表</span><span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">Customer</span> <span class="hljs-keyword">extends</span> \<span class="hljs-title">yii</span>\<span class="hljs-title">db</span>\<span class="hljs-title">ActiveRecord</span></span><span class="hljs-class"></span>&#123;    <span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> <span class="hljs-function"><span class="hljs-keyword">function</span> <span class="hljs-title">tableName</span><span class="hljs-params">()</span></span><span class="hljs-function">    </span>&#123;        <span class="hljs-keyword">return</span> <span class="hljs-string">'customer'</span>;    &#125;    <span class="hljs-keyword">public</span> <span class="hljs-function"><span class="hljs-keyword">function</span> <span class="hljs-title">getComments</span><span class="hljs-params">()</span></span><span class="hljs-function">    </span>&#123;        <span class="hljs-comment">// 一个 customer 有很多条评论（comments）</span>        <span class="hljs-keyword">return</span> <span class="hljs-keyword">$this</span>-&gt;hasMany(Comment::className(), [<span class="hljs-string">'customer_id'</span> =&gt; <span class="hljs-string">'id'</span>]);    &#125;&#125;<span class="hljs-comment">// Comment 对应的是 MongoDB 数据库中的  "comment" 集合（译者注：MongoDB 中的集合相当于 MySQL 中的表）</span><span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">Comment</span> <span class="hljs-keyword">extends</span> \<span class="hljs-title">yii</span>\<span class="hljs-title">mongodb</span>\<span class="hljs-title">ActiveRecord</span></span><span class="hljs-class"></span>&#123;    <span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> <span class="hljs-function"><span class="hljs-keyword">function</span> <span class="hljs-title">collectionName</span><span class="hljs-params">()</span></span><span class="hljs-function">    </span>&#123;        <span class="hljs-keyword">return</span> <span class="hljs-string">'comment'</span>;    &#125;    <span class="hljs-keyword">public</span> <span class="hljs-function"><span class="hljs-keyword">function</span> <span class="hljs-title">getCustomer</span><span class="hljs-params">()</span></span><span class="hljs-function">    </span>&#123;        <span class="hljs-comment">// 一条评论对应一位 customer</span>        <span class="hljs-keyword">return</span> <span class="hljs-keyword">$this</span>-&gt;hasOne(Customer::className(), [<span class="hljs-string">'id'</span> =&gt; <span class="hljs-string">'customer_id'</span>]);    &#125;&#125;$customers = Customer::find()-&gt;with(<span class="hljs-string">'comments'</span>)-&gt;all();<span class="hljs-comment">// 本节中描述的大多数关联查询功能，你都可以抄一抄。</span><span class="hljs-comment">// 注意： joinWith() 这个功能限制于某些数据库是否支持跨数据库 JOIN 查询。  因此，你再上述的代码里就不能用此方法了，因为 MongoDB 不支持 JOIN 查询。</span></code></pre><h2 id="自定义查询类"><a href="#自定义查询类" class="headerlink" title="自定义查询类"></a>自定义查询类</h2><pre><code class="hljs php"><span class="hljs-comment">// 默认情况下，yii\db\ActiveQuery 支持所有 Active Record 查询。要在 Active Record 类中使用自定义的查询类， 您应该重写 yii\db\ActiveRecord::find() 方法并返回一个你自定义查询类的实例。 例如，</span><span class="hljs-comment">// file Comment.php</span><span class="hljs-keyword">namespace</span> <span class="hljs-title">app</span>\<span class="hljs-title">models</span>;<span class="hljs-keyword">use</span> <span class="hljs-title">yii</span>\<span class="hljs-title">db</span>\<span class="hljs-title">ActiveRecord</span>;<span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">Comment</span> <span class="hljs-keyword">extends</span> <span class="hljs-title">ActiveRecord</span></span><span class="hljs-class"></span>&#123;    <span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> <span class="hljs-function"><span class="hljs-keyword">function</span> <span class="hljs-title">find</span><span class="hljs-params">()</span></span><span class="hljs-function">    </span>&#123;        <span class="hljs-keyword">return</span> <span class="hljs-keyword">new</span> CommentQuery(get_called_class());    &#125;&#125;<span class="hljs-comment">// 现在，对于 Comment 类，不管你执行查询（比如 find()、findOne()），还是定义一个关联（比如 hasOne()）， 你都将调用到 CommentQuery 实例，而不再是 ActiveQuery 实例。</span><span class="hljs-comment">// 现在你可以定义 CommentQuery 类了，发挥你的奇技淫巧，以改善查询构建体验。例如，</span><span class="hljs-comment">// file CommentQuery.php</span><span class="hljs-keyword">namespace</span> <span class="hljs-title">app</span>\<span class="hljs-title">models</span>;<span class="hljs-keyword">use</span> <span class="hljs-title">yii</span>\<span class="hljs-title">db</span>\<span class="hljs-title">ActiveQuery</span>;<span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">CommentQuery</span> <span class="hljs-keyword">extends</span> <span class="hljs-title">ActiveQuery</span></span><span class="hljs-class"></span>&#123;    <span class="hljs-comment">// 默认加上一些条件（可以跳过）</span>    <span class="hljs-keyword">public</span> <span class="hljs-function"><span class="hljs-keyword">function</span> <span class="hljs-title">init</span><span class="hljs-params">()</span></span><span class="hljs-function">    </span>&#123;        <span class="hljs-keyword">$this</span>-&gt;andOnCondition([<span class="hljs-string">'deleted'</span> =&gt; <span class="hljs-keyword">false</span>]);        <span class="hljs-keyword">parent</span>::init();    &#125;    <span class="hljs-comment">// ... 在这里加上自定义的查询方法 ...</span>    <span class="hljs-keyword">public</span> <span class="hljs-function"><span class="hljs-keyword">function</span> <span class="hljs-title">active</span><span class="hljs-params">($state = true)</span></span><span class="hljs-function">    </span>&#123;        <span class="hljs-keyword">return</span> <span class="hljs-keyword">$this</span>-&gt;andOnCondition([<span class="hljs-string">'active'</span> =&gt; $state]);    &#125;&#125;<span class="hljs-comment">// 注意： 作为 onCondition() 方法的替代方案，你应当调用 andOnCondition() 或 orOnCondition() 方法来附加新增的条件， 不然在一个新定义的查询方法，已存在的条件可能会被覆盖。</span><span class="hljs-comment">// 然后你就可以先下面这样构建你的查询了：</span>$comments = Comment::find()-&gt;active()-&gt;all();$inactiveComments = Comment::find()-&gt;active(<span class="hljs-keyword">false</span>)-&gt;all();<span class="hljs-comment">// 提示： 在大型项目中，建议您使用自定义查询类来容纳大多数与查询相关的代码， 以使 Active Record 类保持简洁。</span><span class="hljs-comment">// 您还可以在 Comment 关联关系的定义中或在执行关联查询时，使用刚刚新建查询构建方法：</span><span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">Customer</span> <span class="hljs-keyword">extends</span> \<span class="hljs-title">yii</span>\<span class="hljs-title">db</span>\<span class="hljs-title">ActiveRecord</span></span><span class="hljs-class"></span>&#123;    <span class="hljs-keyword">public</span> <span class="hljs-function"><span class="hljs-keyword">function</span> <span class="hljs-title">getActiveComments</span><span class="hljs-params">()</span></span><span class="hljs-function">    </span>&#123;        <span class="hljs-keyword">return</span> <span class="hljs-keyword">$this</span>-&gt;hasMany(Comment::className(), [<span class="hljs-string">'customer_id'</span> =&gt; <span class="hljs-string">'id'</span>])-&gt;active();    &#125;&#125;$customers = Customer::find()-&gt;joinWith(<span class="hljs-string">'activeComments'</span>)-&gt;all();<span class="hljs-comment">// 或者这样</span><span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">Customer</span> <span class="hljs-keyword">extends</span> \<span class="hljs-title">yii</span>\<span class="hljs-title">db</span>\<span class="hljs-title">ActiveRecord</span></span><span class="hljs-class"></span>&#123;    <span class="hljs-keyword">public</span> <span class="hljs-function"><span class="hljs-keyword">function</span> <span class="hljs-title">getComments</span><span class="hljs-params">()</span></span><span class="hljs-function">    </span>&#123;        <span class="hljs-keyword">return</span> <span class="hljs-keyword">$this</span>-&gt;hasMany(Comment::className(), [<span class="hljs-string">'customer_id'</span> =&gt; <span class="hljs-string">'id'</span>]);    &#125;&#125;$customers = Customer::find()-&gt;joinWith([    <span class="hljs-string">'comments'</span> =&gt; <span class="hljs-function"><span class="hljs-keyword">function</span><span class="hljs-params">($q)</span> </span>&#123;        $q-&gt;active();    &#125;])-&gt;all();<span class="hljs-comment">// 提示： 在 Yii 1.1 中，有个概念叫做 命名范围。命名范围在 Yii 2.0 中不再支持， 你依然可以使用自定义查询类、查询方法来达到一样的效果。</span></code></pre><h2 id="选择额外的字段"><a href="#选择额外的字段" class="headerlink" title="选择额外的字段"></a>选择额外的字段</h2><pre><code class="hljs php"><span class="hljs-comment">// 当 Active Record 实例从查询结果中填充时，从数据结果集中， 其属性的值将被相应的列填充。</span><span class="hljs-comment">// 你可以从查询中获取其他列或值，并将其存储在 Active Record 活动记录中。 例如，假设我们有一个名为 room 的表，其中包含有关酒店可用房间的信息。 每个房间使用字段 length，width，height 存储有关其空间大小的信息。 想象一下，我们需要检索出所有可用房间的列表，并按照体积大小倒序排列。 你不可能使用 PHP 来计算体积，但是，由于我们需要按照它的值对这些记录进行排序，你依然需要 volume （体积） 来显示在这个列表中。 为了达到这个目标，你需要在你的 Room 活动记录类中声明一个额外的字段，它将存储 volume 的值：</span><span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">Room</span> <span class="hljs-keyword">extends</span> \<span class="hljs-title">yii</span>\<span class="hljs-title">db</span>\<span class="hljs-title">ActiveRecord</span></span><span class="hljs-class"></span>&#123;    <span class="hljs-keyword">public</span> $volume;    <span class="hljs-comment">// ...</span>&#125;<span class="hljs-comment">// 然后，你需要撰写一个查询，它可以计算房间的大小并执行排序：</span>$rooms = Room::find()    -&gt;select([        <span class="hljs-string">'&#123;&#123;room&#125;&#125;.*'</span>, <span class="hljs-comment">// select all columns</span>        <span class="hljs-string">'([[length]] * [[width]] * [[height]]) AS volume'</span>, <span class="hljs-comment">// 计算体积</span>    ])    -&gt;orderBy(<span class="hljs-string">'volume DESC'</span>) <span class="hljs-comment">// 使用排序</span>    -&gt;all();<span class="hljs-keyword">foreach</span> ($rooms <span class="hljs-keyword">as</span> $room) &#123;    <span class="hljs-keyword">echo</span> $room-&gt;volume; <span class="hljs-comment">// 包含了由 SQL 计算出的值</span>&#125;<span class="hljs-comment">// 额外字段的特性对于聚合查询非常有用。 假设您需要显示一系列客户的订单数量。 首先，您需要使用 orders 关系声明一个 Customer 类，并指定额外字段来存储 count 结果：</span><span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">Customer</span> <span class="hljs-keyword">extends</span> \<span class="hljs-title">yii</span>\<span class="hljs-title">db</span>\<span class="hljs-title">ActiveRecord</span></span><span class="hljs-class"></span>&#123;    <span class="hljs-keyword">public</span> $ordersCount;    <span class="hljs-comment">// ...</span>    <span class="hljs-keyword">public</span> <span class="hljs-function"><span class="hljs-keyword">function</span> <span class="hljs-title">getOrders</span><span class="hljs-params">()</span></span><span class="hljs-function">    </span>&#123;        <span class="hljs-keyword">return</span> <span class="hljs-keyword">$this</span>-&gt;hasMany(Order::className(), [<span class="hljs-string">'customer_id'</span> =&gt; <span class="hljs-string">'id'</span>]);    &#125;&#125;<span class="hljs-comment">// 然后你可以编写一个查询来 JOIN 订单表，并计算订单的总数：</span>$customers = Customer::find()    -&gt;select([        <span class="hljs-string">'&#123;&#123;customer&#125;&#125;.*'</span>, <span class="hljs-comment">// select customer 表所有的字段</span>        <span class="hljs-string">'COUNT(&#123;&#123;order&#125;&#125;.id) AS ordersCount'</span> <span class="hljs-comment">// 计算订单总数</span>    ])    -&gt;joinWith(<span class="hljs-string">'orders'</span>) <span class="hljs-comment">// 连接表</span>    -&gt;groupBy(<span class="hljs-string">'&#123;&#123;customer&#125;&#125;.id'</span>) <span class="hljs-comment">// 分组查询，以确保聚合函数生效</span>    -&gt;all();<span class="hljs-comment">// 使用此方法的一个缺点是，如果数据不是从 SQL 查询上加载的，它必须再单独计算一遍。 因此，如果你通过常规查询获取个别的数据记录时，它没有额外的 select 语句，那么它 将无法返回额外字段的实际值。新保存的记录一样会发生这种情。</span>$room = <span class="hljs-keyword">new</span> Room();$room-&gt;length = <span class="hljs-number">100</span>;$room-&gt;width = <span class="hljs-number">50</span>;$room-&gt;height = <span class="hljs-number">2</span>;$room-&gt;volume; <span class="hljs-comment">// 为 `null`, 因为它没有被声明（赋值）</span><span class="hljs-comment">// 通过 __get() 和 __set() 魔术方法 我们可以将属性赋予行为特性：</span><span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">Room</span> <span class="hljs-keyword">extends</span> \<span class="hljs-title">yii</span>\<span class="hljs-title">db</span>\<span class="hljs-title">ActiveRecord</span></span><span class="hljs-class"></span>&#123;    <span class="hljs-keyword">private</span> $_volume;        <span class="hljs-keyword">public</span> <span class="hljs-function"><span class="hljs-keyword">function</span> <span class="hljs-title">setVolume</span><span class="hljs-params">($volume)</span></span><span class="hljs-function">    </span>&#123;        <span class="hljs-keyword">$this</span>-&gt;_volume = (float) $volume;    &#125;        <span class="hljs-keyword">public</span> <span class="hljs-function"><span class="hljs-keyword">function</span> <span class="hljs-title">getVolume</span><span class="hljs-params">()</span></span><span class="hljs-function">    </span>&#123;        <span class="hljs-keyword">if</span> (<span class="hljs-keyword">empty</span>(<span class="hljs-keyword">$this</span>-&gt;length) || <span class="hljs-keyword">empty</span>(<span class="hljs-keyword">$this</span>-&gt;width) || <span class="hljs-keyword">empty</span>(<span class="hljs-keyword">$this</span>-&gt;height)) &#123;            <span class="hljs-keyword">return</span> <span class="hljs-keyword">null</span>;        &#125;                <span class="hljs-keyword">if</span> (<span class="hljs-keyword">$this</span>-&gt;_volume === <span class="hljs-keyword">null</span>) &#123;            <span class="hljs-keyword">$this</span>-&gt;setVolume(                <span class="hljs-keyword">$this</span>-&gt;length * <span class="hljs-keyword">$this</span>-&gt;width * <span class="hljs-keyword">$this</span>-&gt;height            );        &#125;                <span class="hljs-keyword">return</span> <span class="hljs-keyword">$this</span>-&gt;_volume;    &#125;    <span class="hljs-comment">// ...</span>&#125;<span class="hljs-comment">// 当 select 查询不提供 volume 体积时，这模型将能够自动计算体积的值出来， 当访问模型的属性的时候。</span><span class="hljs-comment">// 当定义关联关系的时候，你也可以计算聚合字段：</span><span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">Customer</span> <span class="hljs-keyword">extends</span> \<span class="hljs-title">yii</span>\<span class="hljs-title">db</span>\<span class="hljs-title">ActiveRecord</span></span><span class="hljs-class"></span>&#123;    <span class="hljs-keyword">private</span> $_ordersCount;    <span class="hljs-keyword">public</span> <span class="hljs-function"><span class="hljs-keyword">function</span> <span class="hljs-title">setOrdersCount</span><span class="hljs-params">($count)</span></span><span class="hljs-function">    </span>&#123;        <span class="hljs-keyword">$this</span>-&gt;_ordersCount = (int) $count;    &#125;    <span class="hljs-keyword">public</span> <span class="hljs-function"><span class="hljs-keyword">function</span> <span class="hljs-title">getOrdersCount</span><span class="hljs-params">()</span></span><span class="hljs-function">    </span>&#123;        <span class="hljs-keyword">if</span> (<span class="hljs-keyword">$this</span>-&gt;isNewRecord) &#123;            <span class="hljs-keyword">return</span> <span class="hljs-keyword">null</span>; <span class="hljs-comment">// 这样可以避免调用空主键进行查询</span>        &#125;        <span class="hljs-keyword">if</span> (<span class="hljs-keyword">$this</span>-&gt;_ordersCount === <span class="hljs-keyword">null</span>) &#123;            <span class="hljs-keyword">$this</span>-&gt;setOrdersCount(<span class="hljs-keyword">$this</span>-&gt;getOrders()-&gt;count()); <span class="hljs-comment">// 根据关联关系按需计算聚合字段</span>        &#125;        <span class="hljs-keyword">return</span> <span class="hljs-keyword">$this</span>-&gt;_ordersCount;    &#125;    <span class="hljs-comment">// ...</span>    <span class="hljs-keyword">public</span> <span class="hljs-function"><span class="hljs-keyword">function</span> <span class="hljs-title">getOrders</span><span class="hljs-params">()</span></span><span class="hljs-function">    </span>&#123;        <span class="hljs-keyword">return</span> <span class="hljs-keyword">$this</span>-&gt;hasMany(Order::className(), [<span class="hljs-string">'customer_id'</span> =&gt; <span class="hljs-string">'id'</span>]);    &#125;&#125;<span class="hljs-comment">// 使用此代码，如果 'select' 语句中存在 'ordersCount' - 它会从查询结果集获取数据填充 Customer::ordersCount 属性， 否则它会在被访问的时候，使用 Customer::orders 关联按需计算。</span><span class="hljs-comment">// 这种方法也适用于创建一些关联数据的快捷访问方式，特别是对于聚合。 例如：</span><span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">Customer</span> <span class="hljs-keyword">extends</span> \<span class="hljs-title">yii</span>\<span class="hljs-title">db</span>\<span class="hljs-title">ActiveRecord</span></span><span class="hljs-class"></span>&#123;    <span class="hljs-comment">/**</span><span class="hljs-comment">     * 为聚合数据定义一个只读的虚拟属性</span><span class="hljs-comment">     */</span>    <span class="hljs-keyword">public</span> <span class="hljs-function"><span class="hljs-keyword">function</span> <span class="hljs-title">getOrdersCount</span><span class="hljs-params">()</span></span><span class="hljs-function">    </span>&#123;        <span class="hljs-keyword">if</span> (<span class="hljs-keyword">$this</span>-&gt;isNewRecord) &#123;            <span class="hljs-keyword">return</span> <span class="hljs-keyword">null</span>; <span class="hljs-comment">//  这样可以避免调用空主键进行查询</span>        &#125;                <span class="hljs-keyword">return</span> <span class="hljs-keyword">empty</span>(<span class="hljs-keyword">$this</span>-&gt;ordersAggregation) ? <span class="hljs-number">0</span> : <span class="hljs-keyword">$this</span>-&gt;ordersAggregation[<span class="hljs-number">0</span>][<span class="hljs-string">'counted'</span>];    &#125;    <span class="hljs-comment">/**</span><span class="hljs-comment">     * 声明一个常规的 'orders' 关联</span><span class="hljs-comment">     */</span>    <span class="hljs-keyword">public</span> <span class="hljs-function"><span class="hljs-keyword">function</span> <span class="hljs-title">getOrders</span><span class="hljs-params">()</span></span><span class="hljs-function">    </span>&#123;        <span class="hljs-keyword">return</span> <span class="hljs-keyword">$this</span>-&gt;hasMany(Order::className(), [<span class="hljs-string">'customer_id'</span> =&gt; <span class="hljs-string">'id'</span>]);    &#125;    <span class="hljs-comment">/**</span><span class="hljs-comment">     * 基于 'orders' 关联，声明一个用于查询聚合的新关联</span><span class="hljs-comment">     */</span>    <span class="hljs-keyword">public</span> <span class="hljs-function"><span class="hljs-keyword">function</span> <span class="hljs-title">getOrdersAggregation</span><span class="hljs-params">()</span></span><span class="hljs-function">    </span>&#123;        <span class="hljs-keyword">return</span> <span class="hljs-keyword">$this</span>-&gt;getOrders()            -&gt;select([<span class="hljs-string">'customer_id'</span>, <span class="hljs-string">'counted'</span> =&gt; <span class="hljs-string">'count(*)'</span>])            -&gt;groupBy(<span class="hljs-string">'customer_id'</span>)            -&gt;asArray(<span class="hljs-keyword">true</span>);    &#125;    <span class="hljs-comment">// ...</span>&#125;<span class="hljs-keyword">foreach</span> (Customer::find()-&gt;with(<span class="hljs-string">'ordersAggregation'</span>)-&gt;all() <span class="hljs-keyword">as</span> $customer) &#123;    <span class="hljs-keyword">echo</span> $customer-&gt;ordersCount; <span class="hljs-comment">// 输出关联的聚合数据，而不需要额外的查询，因为我们用了即时加载</span>&#125;$customer = Customer::findOne($pk);$customer-&gt;ordersCount; <span class="hljs-comment">// 从延迟加载的关联中，输出聚合数据</span></code></pre><p>————————————————<br>文章来源于Yii官方文档：<a href="https://www.yiiframework.com/doc/guide/2.0/zh-cn/db-active-record" target="_blank" rel="noopener">活动记录（Active Record）</a> </p>]]></content>
    
    
    <categories>
      
      <category>PHP</category>
      
    </categories>
    
    
    <tags>
      
      <tag>PHP</tag>
      
      <tag>Yii</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>PHP 7 新特性</title>
    <link href="/a8d34c6a.html"/>
    <url>/a8d34c6a.html</url>
    
    <content type="html"><![CDATA[<h1 id="标量类型声明"><a href="#标量类型声明" class="headerlink" title="标量类型声明"></a>标量类型声明</h1><p>标量类型声明 有两种模式: 强制 (默认) 和 严格模式。 现在可以使用下列类型参数（无论用强制模式还是严格模式）： 字符串 (string), 整数 (int), 浮点数 (float), 以及布尔值 (bool)。它们扩充了PHP5中引入的其他类型：类名，接口，数组和 回调类型。</p><pre><code class="hljs php"><span class="hljs-comment">// 强制模式</span><span class="hljs-function"><span class="hljs-keyword">function</span> <span class="hljs-title">sumOfInts</span><span class="hljs-params">(int ...$ints)</span></span><span class="hljs-function"></span>&#123;    <span class="hljs-keyword">return</span> array_sum($ints);&#125;var_dump(sumOfInts(<span class="hljs-number">2</span>, <span class="hljs-string">'3'</span>, <span class="hljs-number">4.1</span>));</code></pre><p>以上例程会输出：</p><pre><code>int(9)</code></pre><p>要使用严格模式，一个 declare 声明指令必须放在文件的顶部。这意味着严格声明标量是基于文件可配的。 这个指令不仅影响参数的类型声明，也影响到函数的返回值声明]</p><pre><code class="hljs php"><span class="hljs-comment">// 严格模式</span><span class="hljs-keyword">declare</span>(strict_types=<span class="hljs-number">1</span>);<span class="hljs-function"><span class="hljs-keyword">function</span> <span class="hljs-title">sumOfInts</span><span class="hljs-params">(int ...$ints)</span></span><span class="hljs-function"></span>&#123;    <span class="hljs-keyword">return</span> array_sum($ints);&#125;var_dump(sumOfInts(<span class="hljs-number">2</span>, <span class="hljs-string">'3'</span>, <span class="hljs-number">4.1</span>));</code></pre><p>以上程序由于采用了严格模式，所以如果参数中出现不适整数的类型会报错，执行输出结果为：</p><pre><code>PHP Fatal error:  Uncaught TypeError: Argument 2 passed to sumOfInts() must be of the type integer, string given...</code></pre><h1 id="返回值类型声明"><a href="#返回值类型声明" class="headerlink" title="返回值类型声明"></a>返回值类型声明</h1><p>PHP 7 增加了对返回类型声明的支持。 类似于参数类型声明，返回类型声明指明了函数返回值的类型。可用的类型与参数声明中可用的类型相同。</p><pre><code class="hljs php"><span class="hljs-function"><span class="hljs-keyword">function</span> <span class="hljs-title">arraysSum</span><span class="hljs-params">(array ...$arrays)</span>: <span class="hljs-title">array</span></span><span class="hljs-function"></span>&#123;    <span class="hljs-keyword">return</span> array_map(<span class="hljs-function"><span class="hljs-keyword">function</span><span class="hljs-params">(array $array)</span>: <span class="hljs-title">int</span> </span>&#123;        <span class="hljs-keyword">return</span> array_sum($array);    &#125;, $arrays);&#125;print_r(arraysSum([<span class="hljs-number">1</span>,<span class="hljs-number">2</span>,<span class="hljs-number">3</span>], [<span class="hljs-number">4</span>,<span class="hljs-number">5</span>,<span class="hljs-number">6</span>], [<span class="hljs-number">7</span>,<span class="hljs-number">8</span>,<span class="hljs-number">9</span>]));</code></pre><p>以上例程会输出：</p><pre><code>Array(    [0] =&gt; 6    [1] =&gt; 15    [2] =&gt; 24)</code></pre><h1 id="null合并运算符"><a href="#null合并运算符" class="headerlink" title="null合并运算符"></a>null合并运算符</h1><p>由于日常使用中存在大量同时使用三元表达式和 isset()的情况， 我们添加了 null 合并运算符 (??) 这个语法糖。如果变量存在且值不为 NULL， 它就会返回自身的值，否则返回它的第二个操作数。</p><pre><code class="hljs php"><span class="hljs-comment">// 如果$_GET['user']存在且不为空则返回它自己，否则返回合并运算符后面的值</span>$username = $_GET[<span class="hljs-string">'user'</span>] ?? <span class="hljs-string">'nobody'</span>;<span class="hljs-comment">// 相当于:</span>$username = <span class="hljs-keyword">isset</span>($_GET[<span class="hljs-string">'user'</span>]) ? $_GET[<span class="hljs-string">'user'</span>] : <span class="hljs-string">'nobody'</span>;<span class="hljs-comment">// 多个合并运算符可以一起使用</span>$username = $_GET[<span class="hljs-string">'user'</span>] ?? $_POST[<span class="hljs-string">'user'</span>] ?? <span class="hljs-string">'nobody'</span>;</code></pre><h1 id="太空船操作符（组合比较符）"><a href="#太空船操作符（组合比较符）" class="headerlink" title="太空船操作符（组合比较符）"></a>太空船操作符（组合比较符）</h1><p>太空船操作符用于比较两个表达式。当 $a 小于、等于或大于 $b 时它分别返回 -1  0 或 1。 比较的原则是沿用 PHP 的常规比较规则进行的。</p><pre><code class="hljs php"><span class="hljs-comment">// 整数</span><span class="hljs-keyword">echo</span> <span class="hljs-number">1</span> &lt;=&gt; <span class="hljs-number">1</span>; <span class="hljs-comment">// 0</span><span class="hljs-keyword">echo</span> <span class="hljs-number">1</span> &lt;=&gt; <span class="hljs-number">2</span>; <span class="hljs-comment">// -1</span><span class="hljs-keyword">echo</span> <span class="hljs-number">2</span> &lt;=&gt; <span class="hljs-number">1</span>; <span class="hljs-comment">// 1</span><span class="hljs-comment">// 浮点数</span><span class="hljs-keyword">echo</span> <span class="hljs-number">1.5</span> &lt;=&gt; <span class="hljs-number">1.5</span>; <span class="hljs-comment">// 0</span><span class="hljs-keyword">echo</span> <span class="hljs-number">1.5</span> &lt;=&gt; <span class="hljs-number">2.5</span>; <span class="hljs-comment">// -1</span><span class="hljs-keyword">echo</span> <span class="hljs-number">2.5</span> &lt;=&gt; <span class="hljs-number">1.5</span>; <span class="hljs-comment">// 1</span> <span class="hljs-comment">// 字符串</span><span class="hljs-keyword">echo</span> <span class="hljs-string">"a"</span> &lt;=&gt; <span class="hljs-string">"a"</span>; <span class="hljs-comment">// 0</span><span class="hljs-keyword">echo</span> <span class="hljs-string">"a"</span> &lt;=&gt; <span class="hljs-string">"b"</span>; <span class="hljs-comment">// -1</span><span class="hljs-keyword">echo</span> <span class="hljs-string">"b"</span> &lt;=&gt; <span class="hljs-string">"a"</span>; <span class="hljs-comment">// 1</span></code></pre><h1 id="通过-define-定义常量数组"><a href="#通过-define-定义常量数组" class="headerlink" title="通过 define() 定义常量数组"></a>通过 define() 定义常量数组</h1><p>Array 类型的常量现在可以通过 define() 来定义。在 PHP5.6 中仅能通过 const 定义。</p><pre><code class="hljs php">define(<span class="hljs-string">'ANIMALS'</span>, [    <span class="hljs-string">'dog'</span>,    <span class="hljs-string">'cat'</span>,    <span class="hljs-string">'bird'</span>]);<span class="hljs-keyword">echo</span> ANIMALS[<span class="hljs-number">1</span>]; <span class="hljs-comment">// 输出 "cat"</span></code></pre><h1 id="匿名类"><a href="#匿名类" class="headerlink" title="匿名类"></a>匿名类</h1><p>现在支持通过 new class 来实例化一个匿名类，这可以用来替代一些“用后即焚”的完整类定义。</p><pre><code class="hljs php"><span class="hljs-class"><span class="hljs-keyword">interface</span> <span class="hljs-title">Logger</span> </span>&#123;    <span class="hljs-keyword">public</span> <span class="hljs-function"><span class="hljs-keyword">function</span> <span class="hljs-title">log</span><span class="hljs-params">(string $msg)</span></span>;&#125;<span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">Application</span> </span>&#123;    <span class="hljs-keyword">private</span> $logger;    <span class="hljs-keyword">public</span> <span class="hljs-function"><span class="hljs-keyword">function</span> <span class="hljs-title">getLogger</span><span class="hljs-params">()</span>: <span class="hljs-title">Logger</span> </span>&#123;         <span class="hljs-keyword">return</span> <span class="hljs-keyword">$this</span>-&gt;logger;    &#125;    <span class="hljs-keyword">public</span> <span class="hljs-function"><span class="hljs-keyword">function</span> <span class="hljs-title">setLogger</span><span class="hljs-params">(Logger $logger)</span> </span>&#123;         <span class="hljs-keyword">$this</span>-&gt;logger = $logger;    &#125;&#125;$app = <span class="hljs-keyword">new</span> Application;$app-&gt;setLogger(<span class="hljs-keyword">new</span> <span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-keyword">implements</span> <span class="hljs-title">Logger</span> </span>&#123;    <span class="hljs-keyword">public</span> <span class="hljs-function"><span class="hljs-keyword">function</span> <span class="hljs-title">log</span><span class="hljs-params">(string $msg)</span> </span>&#123;        <span class="hljs-keyword">echo</span> $msg;    &#125;&#125;);var_dump($app-&gt;getLogger());</code></pre><p>以上例程会输出：</p><pre><code>object(class@anonymous)#2 (0) {}</code></pre><h1 id="Unicode-codepoint-转译语法"><a href="#Unicode-codepoint-转译语法" class="headerlink" title="Unicode codepoint 转译语法"></a>Unicode codepoint 转译语法</h1><p>这接受一个以16进制形式的 Unicode codepoint，并打印出一个双引号或 heredoc 包围的 UTF-8 编码格式的字符串。 可以接受任何有效的 codepoint，并且开头的 0 是可以省略的。</p><p>echo “\u{aa}”;<br>echo “\u{0000aa}”;<br>echo “\u{9999}”;<br>以上例程会输出：</p><pre><code>ªª (same as before but with optional leading 0&apos;s)香</code></pre><h1 id="Closure-call"><a href="#Closure-call" class="headerlink" title="Closure::call()"></a>Closure::call()</h1><p>Closure::call() 现在有着更好的性能，简短干练的暂时绑定一个方法到对象上闭包并调用它。</p><pre><code class="hljs php"><span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">A</span> </span>&#123;<span class="hljs-keyword">private</span> $x = <span class="hljs-number">1</span>;&#125;<span class="hljs-comment">// PHP 7 之前版本的代码</span>$getXCB = <span class="hljs-function"><span class="hljs-keyword">function</span><span class="hljs-params">()</span> </span>&#123;<span class="hljs-keyword">return</span> <span class="hljs-keyword">$this</span>-&gt;x;&#125;;$getX = $getXCB-&gt;bindTo(<span class="hljs-keyword">new</span> A, <span class="hljs-string">'A'</span>); <span class="hljs-comment">// 中间层闭包</span><span class="hljs-keyword">echo</span> $getX();<span class="hljs-comment">// PHP 7+ 及更高版本的代码</span>$getX = <span class="hljs-function"><span class="hljs-keyword">function</span><span class="hljs-params">()</span> </span>&#123;<span class="hljs-keyword">return</span> <span class="hljs-keyword">$this</span>-&gt;x;&#125;;<span class="hljs-keyword">echo</span> $getX-&gt;call(<span class="hljs-keyword">new</span> A);</code></pre><p>以上例程会输出：</p><pre><code>11</code></pre><h1 id="为unserialize-提供过滤"><a href="#为unserialize-提供过滤" class="headerlink" title="为unserialize()提供过滤"></a>为unserialize()提供过滤</h1><p>这个特性旨在提供更安全的方式解包不可靠的数据。它通过白名单的方式来防止潜在的代码注入。</p><pre><code class="hljs php"><span class="hljs-comment">// 将所有的对象都转换为 __PHP_Incomplete_Class 对象</span>$data = unserialize($foo, [<span class="hljs-string">"allowed_classes"</span> =&gt; <span class="hljs-keyword">false</span>]);<span class="hljs-comment">// 将除 MyClass 和 MyClass2 之外的所有对象都转换为 __PHP_Incomplete_Class 对象</span>$data = unserialize($foo, [<span class="hljs-string">"allowed_classes"</span> =&gt; [<span class="hljs-string">"MyClass"</span>, <span class="hljs-string">"MyClass2"</span>]);<span class="hljs-comment">// 默认情况下所有的类都是可接受的，等同于省略第二个参数</span>$data = unserialize($foo, [<span class="hljs-string">"allowed_classes"</span> =&gt; <span class="hljs-keyword">true</span>]);</code></pre><h1 id="IntlChar"><a href="#IntlChar" class="headerlink" title="IntlChar"></a>IntlChar</h1><p>新增加的 IntlChar 类旨在暴露出更多的 ICU 功能。这个类自身定义了许多静态方法用于操作多字符集的 unicode 字符。</p><pre><code class="hljs php">printf(<span class="hljs-string">'%x'</span>, IntlChar::CODEPOINT_MAX);<span class="hljs-keyword">echo</span> IntlChar::charName(<span class="hljs-string">'@'</span>);var_dump(IntlChar::ispunct(<span class="hljs-string">'!'</span>));</code></pre><p>以上例程会输出：</p><pre><code>10ffffCOMMERCIAL ATbool(true)</code></pre><p>若要使用此类，请先安装Intl扩展</p><h1 id="预期"><a href="#预期" class="headerlink" title="预期"></a>预期</h1><p>预期是向后兼用并增强之前的 assert() 的方法。 它使得在生产环境中启用断言为零成本，并且提供当断言失败时抛出特定异常的能力。</p><p>老版本的API出于兼容目的将继续被维护，assert()现在是一个语言结构，它允许第一个参数是一个表达式，而不仅仅是一个待计算的 string或一个待测试的boolean。</p><pre><code class="hljs php">ini_set(<span class="hljs-string">'assert.exception'</span>, <span class="hljs-number">1</span>);<span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">CustomError</span> <span class="hljs-keyword">extends</span> <span class="hljs-title">AssertionError</span> </span>&#123;&#125;assert(<span class="hljs-keyword">false</span>, <span class="hljs-keyword">new</span> CustomError(<span class="hljs-string">'Some error message'</span>));</code></pre><p>以上例程会输出：</p><pre><code>Fatal error: Uncaught CustomError: Some error message</code></pre><h1 id="Group-use-declarations"><a href="#Group-use-declarations" class="headerlink" title="Group use declarations"></a>Group use declarations</h1><p>从同一 namespace 导入的类、函数和常量现在可以通过单个 use 语句 一次性导入了。</p><pre><code class="hljs php"><span class="hljs-comment">// PHP 7 之前的代码</span><span class="hljs-keyword">use</span> <span class="hljs-title">some</span>\<span class="hljs-title">namespace</span>\<span class="hljs-title">ClassA</span>;<span class="hljs-keyword">use</span> <span class="hljs-title">some</span>\<span class="hljs-title">namespace</span>\<span class="hljs-title">ClassB</span>;<span class="hljs-keyword">use</span> <span class="hljs-title">some</span>\<span class="hljs-title">namespace</span>\<span class="hljs-title">ClassC</span> <span class="hljs-title">as</span> <span class="hljs-title">C</span>;<span class="hljs-keyword">use</span> <span class="hljs-title">function</span> <span class="hljs-title">some</span>\<span class="hljs-title">namespace</span>\<span class="hljs-title">fn_a</span>;<span class="hljs-keyword">use</span> <span class="hljs-title">function</span> <span class="hljs-title">some</span>\<span class="hljs-title">namespace</span>\<span class="hljs-title">fn_b</span>;<span class="hljs-keyword">use</span> <span class="hljs-title">function</span> <span class="hljs-title">some</span>\<span class="hljs-title">namespace</span>\<span class="hljs-title">fn_c</span>;<span class="hljs-keyword">use</span> <span class="hljs-title">const</span> <span class="hljs-title">some</span>\<span class="hljs-title">namespace</span>\<span class="hljs-title">ConstA</span>;<span class="hljs-keyword">use</span> <span class="hljs-title">const</span> <span class="hljs-title">some</span>\<span class="hljs-title">namespace</span>\<span class="hljs-title">ConstB</span>;<span class="hljs-keyword">use</span> <span class="hljs-title">const</span> <span class="hljs-title">some</span>\<span class="hljs-title">namespace</span>\<span class="hljs-title">ConstC</span>;<span class="hljs-comment">// PHP 7+ 及更高版本的代码</span><span class="hljs-keyword">use</span> <span class="hljs-title">some</span>\<span class="hljs-title">namespace</span>\&#123;<span class="hljs-title">ClassA</span>, <span class="hljs-title">ClassB</span>, <span class="hljs-title">ClassC</span> <span class="hljs-title">as</span> <span class="hljs-title">C</span>&#125;;<span class="hljs-keyword">use</span> <span class="hljs-title">function</span> <span class="hljs-title">some</span>\<span class="hljs-title">namespace</span>\&#123;<span class="hljs-title">fn_a</span>, <span class="hljs-title">fn_b</span>, <span class="hljs-title">fn_c</span>&#125;;<span class="hljs-keyword">use</span> <span class="hljs-title">const</span> <span class="hljs-title">some</span>\<span class="hljs-title">namespace</span>\&#123;<span class="hljs-title">ConstA</span>, <span class="hljs-title">ConstB</span>, <span class="hljs-title">ConstC</span>&#125;;</code></pre><h1 id="生成器可以返回表达式"><a href="#生成器可以返回表达式" class="headerlink" title="生成器可以返回表达式"></a>生成器可以返回表达式</h1><p>此特性基于 PHP 5.5 版本中引入的生成器特性构建的。 它允许在生成器函数中通过使用 return 语法来返回一个表达式 （但是不允许返回引用值）， 可以通过调用 Generator::getReturn() 方法来获取生成器的返回值， 但是这个方法只能在生成器完成产生工作以后调用一次。</p><pre><code class="hljs php">$gen = (<span class="hljs-function"><span class="hljs-keyword">function</span><span class="hljs-params">()</span> </span>&#123;    <span class="hljs-keyword">yield</span> <span class="hljs-number">1</span>;    <span class="hljs-keyword">yield</span> <span class="hljs-number">2</span>;    <span class="hljs-keyword">return</span> <span class="hljs-number">3</span>;&#125;)();<span class="hljs-keyword">foreach</span> ($gen <span class="hljs-keyword">as</span> $val) &#123;    <span class="hljs-keyword">echo</span> $val, PHP_EOL;&#125;<span class="hljs-keyword">echo</span> $gen-&gt;getReturn(), PHP_EOL;</code></pre><p>以上例程会输出：</p><pre><code>123</code></pre><p>在生成器中能够返回最终的值是一个非常便利的特性， 因为它使得调用生成器的客户端代码可以直接得到生成器（或者其他协同计算）的返回值， 相对于之前版本中客户端代码必须先检查生成器是否产生了最终的值然后再进行响应处理 来得方便多了。</p><h1 id="Generator-delegation"><a href="#Generator-delegation" class="headerlink" title="Generator delegation"></a>Generator delegation</h1><p>现在，只需在最外层生成其中使用 yield from， 就可以把一个生成器自动委派给其他的生成器， Traversable 对象或者 array。</p><pre><code class="hljs php"><span class="hljs-function"><span class="hljs-keyword">function</span> <span class="hljs-title">gen</span><span class="hljs-params">()</span></span><span class="hljs-function"></span>&#123;    <span class="hljs-keyword">yield</span> <span class="hljs-number">1</span>;    <span class="hljs-keyword">yield</span> <span class="hljs-number">2</span>;    <span class="hljs-keyword">yield</span> from gen2();&#125;<span class="hljs-function"><span class="hljs-keyword">function</span> <span class="hljs-title">gen2</span><span class="hljs-params">()</span></span><span class="hljs-function"></span>&#123;    <span class="hljs-keyword">yield</span> <span class="hljs-number">3</span>;    <span class="hljs-keyword">yield</span> <span class="hljs-number">4</span>;&#125;<span class="hljs-keyword">foreach</span> (gen() <span class="hljs-keyword">as</span> $val)&#123;    <span class="hljs-keyword">echo</span> $val, PHP_EOL;&#125;</code></pre><p>以上例程会输出：</p><pre><code>1234</code></pre><h1 id="整数除法函数-intdiv"><a href="#整数除法函数-intdiv" class="headerlink" title="整数除法函数 intdiv()"></a>整数除法函数 intdiv()</h1><p>新加的函数 intdiv() 用来进行 整数的除法运算。</p><pre><code class="hljs php">var_dump(intdiv(<span class="hljs-number">10</span>, <span class="hljs-number">3</span>));</code></pre><p>以上例程会输出：</p><pre><code>int(3)</code></pre><h1 id="会话选项"><a href="#会话选项" class="headerlink" title="会话选项"></a>会话选项</h1><p>session_start() 可以接受一个 array 作为参数， 用来覆盖 php.ini 文件中设置的 会话配置选项。</p><p>在调用 session_start() 的时候， 传入的选项参数中也支持 session.lazy_write 行为， 默认情况下这个配置项是打开的。它的作用是控制 PHP 只有在会话中的数据发生变化的时候才 写入会话存储文件，如果会话中的数据没有发生改变，那么 PHP 会在读取完会话数据之后， 立即关闭会话存储文件，不做任何修改，可以通过设置 read_and_close 来实现。</p><p>例如，下列代码设置 session.cache_limiter 为 private，并且在读取完毕会话数据之后马上关闭会话存储文件。</p><pre><code class="hljs php">session_start([    <span class="hljs-string">'cache_limiter'</span> =&gt; <span class="hljs-string">'private'</span>,    <span class="hljs-string">'read_and_close'</span> =&gt; <span class="hljs-keyword">true</span>,]);</code></pre><h1 id="preg-replace-callback-array"><a href="#preg-replace-callback-array" class="headerlink" title="preg_replace_callback_array()"></a>preg_replace_callback_array()</h1><p>在 PHP 7 之前，当使用 preg_replace_callback() 函数的时候， 由于针对每个正则表达式都要执行回调函数，可能导致过多的分支代码。 而使用新加的 preg_replace_callback_array() 函数， 可以使得代码更加简洁。</p><p>现在，可以使用一个关联数组来对每个正则表达式注册回调函数， 正则表达式本身作为关联数组的键， 而对应的回调函数就是关联数组的值。</p><h1 id="CSPRNG-Functions"><a href="#CSPRNG-Functions" class="headerlink" title="CSPRNG Functions"></a>CSPRNG Functions</h1><p>新加入两个跨平台的函数： random_bytes() 和 random_int() 用来产生高安全级别的随机字符串和随机整数。</p><h1 id="可以使用-list-函数来展开实现了-ArrayAccess-接口的对象"><a href="#可以使用-list-函数来展开实现了-ArrayAccess-接口的对象" class="headerlink" title="可以使用 list() 函数来展开实现了 ArrayAccess 接口的对象"></a>可以使用 list() 函数来展开实现了 ArrayAccess 接口的对象</h1><p>在之前版本中，list() 函数不能保证 正确的展开实现了 ArrayAccess 接口的对象， 现在这个问题已经被修复。</p><h1 id="其他特性"><a href="#其他特性" class="headerlink" title="其他特性"></a>其他特性</h1><p>允许在克隆表达式上访问对象成员，例如： (clone $foo)-&gt;bar()。</p><p>————————————————<br>文章来源于PHP官方文档：<a href="https://www.php.net/manual/zh/migration70.new-features.php" target="_blank" rel="noopener">PHP 5.6.x 移植到 PHP 7.0.x</a> </p>]]></content>
    
    
    <categories>
      
      <category>PHP</category>
      
    </categories>
    
    
    <tags>
      
      <tag>PHP</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>安装 LNMP 环境</title>
    <link href="/5ca3d0c3.html"/>
    <url>/5ca3d0c3.html</url>
    
    <content type="html"><![CDATA[<h1 id="安装"><a href="#安装" class="headerlink" title="安装"></a>安装</h1><ul><li><p>虚拟机中安装 centos 8（自行搜索,安装后只有命令行，图形界面需要另外安装）</p></li><li><p>切换 root 账号</p><pre><code>su root</code></pre></li><li><p>使用 <code>dhclient</code> 命令为 centos 8 分配一个 ip 地址</p></li></ul><p><img src="https://cdn.jsdelivr.net/gh/loquy/loquy.github.io/images/1593925975.jpg" srcset="/img/loading.gif" alt=""></p><ul><li><p>使用vim编辑网卡配置，将上面分配所得的ip地址写入配置文件</p><pre><code>vim /etc/sysconfig/network-scripts/ifcfg-enp0s3</code></pre></li><li><p>注意红框的修改的配置按 <code>i</code> 键插入编辑</p></li></ul><p><img src="https://cdn.jsdelivr.net/gh/loquy/loquy.github.io/images/1593924179.jpg" srcset="/img/loading.gif" alt=""></p><ul><li>按 <code>Esc</code> 键然后 <code>:wq</code> 保存退出</li><li>重启网卡使用 <code>nmcli c reload</code> 命令，ping 下百度发现网络可以用了</li></ul><p><img src="https://cdn.jsdelivr.net/gh/loquy/loquy.github.io/images/1593925621.jpg" srcset="/img/loading.gif" alt=""></p><ul><li><p>使用 dnf 取代了 yum 来进行包管理，安装 nginx、php、mysql</p><pre><code>dnf install nginx php mariadb-server php-mysqlnd</code></pre></li><li><p>把 nginx、php-fpm、mariadb 加入到开机启动</p><pre><code>systemctl enable nginxsystemctl enable php-fpmsystemctl enable mariadb</code></pre></li><li><p>启动 nginx、php-fpm、mariadb</p><pre><code>systemctl start nginxsystemctl start php-fpmsystemctl start mariadb</code></pre></li><li><p>设置防火墙开放 tcp 80 和 tcp 3306 端口</p><pre><code>firewall-cmd --zone=public --add-port=80/tcp --permanentfirewall-cmd --zone=public --add-port=3306/tcp --permanentfirewall-cmd --reload</code></pre></li></ul><h1 id="使用"><a href="#使用" class="headerlink" title="使用"></a>使用</h1><h2 id="nginx"><a href="#nginx" class="headerlink" title="nginx"></a>nginx</h2><ul><li>浏览器访问服务器 ip</li></ul><p><img src="https://cdn.jsdelivr.net/gh/loquy/loquy.github.io/images/1593924883.jpg" srcset="/img/loading.gif" alt=""></p><h2 id="mysql"><a href="#mysql" class="headerlink" title="mysql"></a>mysql</h2><ul><li><p>设置 mysql 数据库 root 账号密码</p><pre><code>mysqladmin -uroot  password  &apos;your password&apos;</code></pre></li><li><p>root 账号登陆 mysql</p><pre><code>mysql -uroot -p</code></pre></li><li><p>创建一个新用户 web，密码为 123456，授权远程计算机使用账号 web 登陆数据库，并立刻刷新权限  </p><pre><code>MariaDB[(none)]&gt;grant all on *.* to &apos;web&apos;@&apos;%&apos;  identified by &apos;123456&apos;;MariaDB[(none)]&gt;flush privileges;</code></pre></li><li><p>退出 mysql 数据库</p><pre><code>MariaDB[(none)]&gt; quit; </code></pre></li></ul><p><img src="https://cdn.jsdelivr.net/gh/loquy/loquy.github.io/images/1593925030.jpg" srcset="/img/loading.gif" alt="">    </p><h2 id="php"><a href="#php" class="headerlink" title="php"></a>php</h2><ul><li><p>创建文件 index.php</p><pre><code>touch /usr/share/nginx/html/index.phpecho  &quot;&lt;?php  phpinfo();  ?&gt;&quot; &gt; /usr/share/nginx/html/index.php</code></pre></li><li><p>重启 nginx 服务，使用浏览器访问服务器 ip</p><pre><code>systemctl restart  nginx        </code></pre></li></ul><p><img src="https://cdn.jsdelivr.net/gh/loquy/loquy.github.io/images/1593924755.jpg" srcset="/img/loading.gif" alt="">        </p>]]></content>
    
    
    <categories>
      
      <category>Linux</category>
      
    </categories>
    
    
    <tags>
      
      <tag>Linux</tag>
      
      <tag>LNMP</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>PHP 面试问题整理</title>
    <link href="/babcce42.html"/>
    <url>/babcce42.html</url>
    
    <content type="html"><![CDATA[<h1 id="PHP-的生命周期是什么？"><a href="#PHP-的生命周期是什么？" class="headerlink" title="PHP 的生命周期是什么？"></a>PHP 的生命周期是什么？</h1><p>PHP 生命周期有5个阶段：</p><ul><li><strong>模块初始化阶段</strong> （module init） <ul><li>Zend 引擎的初始化操作。SAPI、FPM（master 进程 ）启动，加载扩展、模块初始化 </li></ul></li><li><strong>请求初始化阶段</strong> （request init） <ul><li>FPM 里 worker 进程接收一个请求并读取请求数据，PHP初始化本次请求所需的环境变量</li></ul></li><li><strong>PHP脚本执行阶段</strong><ul><li>Zend 引擎接将 PHP 代码编译成 opcodes 并顺次执行</li></ul></li><li><strong>请求关闭阶段</strong> （request shutdown） <ul><li>按顺序调用各个模块的 RSHUTDOWN 方法，对每个变量调用 unset 函数</li></ul></li><li><strong>模块关闭阶段</strong> （module shutdown） <ul><li>SAPI 关闭，进行资源的清理，关闭各个模块，关闭 FPM</li></ul></li></ul><h1 id="数据库事务的理解-？"><a href="#数据库事务的理解-？" class="headerlink" title="数据库事务的理解 ？"></a>数据库事务的理解 ？</h1><p> <a href="https://baike.baidu.com/item/数据库/103728" target="_blank" rel="noopener">数据库</a>事务(transaction)是访问并可能操作各种<a href="https://baike.baidu.com/item/数据项/3227309" target="_blank" rel="noopener">数据项</a>的一个数据库操作序列，这些操作要么全部执行,要么全部不执行，是一个不可分割的工作单位。事务由事务开始与事务结束之间执行的全部数据库操作组成。 </p><ul><li>原子性（Atomic）    <ul><li>事务<code>要么全部完成，要么全部取消</code>。 如果事务崩溃，状态回到事务之前（事务回滚）。 </li></ul></li><li>一致性（Consitency）<ul><li>只有合法的数据（依照关系约束和函数约束）才能写入数据库。</li></ul></li><li>隔离性（Isolation）<ul><li>如果2个事务 T1 和 T2 同时运行，事务 T1 和 T2 最终的结果是相同的，不管 T1和T2谁先结束。</li></ul></li><li>持久性（Durability）<ul><li>一旦事务提交，不管发生什么（崩溃或者出错），数据要保存在数据库中。</li></ul></li></ul><h1 id="数据库设计范式是什么-？"><a href="#数据库设计范式是什么-？" class="headerlink" title="数据库设计范式是什么 ？"></a>数据库设计范式是什么 ？</h1><ul><li><p>第一范式（1NF）</p><ul><li>表中的列只能含有原子性(不可再分)的值。</li></ul></li><li><p>第二范式（2NF）</p><ul><li>满足第一范式 ，没有部分依赖 </li></ul></li><li><p>第三范式（3NF）</p><ul><li>满足第二范式，没有传递依赖  </li></ul></li></ul><h1 id="PHP-Swoole是什么？"><a href="#PHP-Swoole是什么？" class="headerlink" title="PHP Swoole是什么？"></a>PHP Swoole是什么？</h1><ul><li>PHP 协程框架</li></ul>  <pre><code class="hljs mipsasm"><span class="hljs-keyword">Swoole </span>使 PHP 开发人员可以编写高性能高并发的 TCP、UDP、Unix Socket、HTTP、 WebSocket 等服务，让 PHP 不再局限于 Web 领域。<span class="hljs-keyword">Swoole4 </span>协程的成熟将 PHP 带入了前所未有的时期， 为性能的提升提供了独一无二的可能性。<span class="hljs-keyword">Swoole </span>可以广泛应用于互联网、移动通信、云计算、 网络游戏、物联网（IOT）、车联网、智能家居等领域。使用 PHP + <span class="hljs-keyword">Swoole </span>可以使企业 IT 研发团队的效率大大提升，更加专注于开发创新产品</code></pre><h1 id="PHP-和-Nginx-怎么交互的？"><a href="#PHP-和-Nginx-怎么交互的？" class="headerlink" title="PHP 和 Nginx 怎么交互的？"></a>PHP 和 Nginx 怎么交互的？</h1><p>Nginx + PHP 的工程模式下，两位主角分工明确，Nginx 负责承载 HTTP 请求的响应与返回，以及超时控制记录日志等 HTTP 相关的功能，而PHP则负责处理具体请求要做的业务逻辑，它们俩的这种合作模式也是常见的分层架构设计中的一种，在它们各有专注面的同时，FastCGI 又很好的将两块衔接，保障上下游通信交互</p><ul><li>发送 HTTP 请求 Nginx 接收</li><li>Nginx 解析到对应 PHP 文件</li><li>加载 Nginx 的 FastCGI 模块启动对应端口</li><li>PHP-FPM 监听对应端口，接收请求，启用 worker 进程处理</li><li>PHP-FPM 处理完请求，返回给 Nginx</li></ul><h1 id="MySQL-存储引擎有什么？"><a href="#MySQL-存储引擎有什么？" class="headerlink" title="MySQL 存储引擎有什么？"></a><strong>MySQL</strong> 存储引擎有什么？</h1><p> 存储引擎其实就是对于数据库文件的一种存取机制  ，如何实现存储数据，如何为存储的数据建立索引以及   如何更新，查询数据等技术实现的方法。 </p><ul><li><strong>InnoDB</strong> <ul><li>优点支持事务处理、崩溃修复能力和并发控制。缺点是读写效率较差，占用空间大。 </li></ul></li><li><strong>MyISAM</strong> <ul><li>优点占用空间小，处理速度快。缺点是不支持事务的完整性和并发性。 </li></ul></li><li><strong>MEMORY</strong><ul><li>所有的数据存储在内存中，关闭机器数据消失。 </li></ul></li><li><strong>ARCHIVE</strong><ul><li>提供了压缩功能，拥有高效的插入速度，不支持索引，所查询性能较差 </li></ul></li></ul><h1 id="PHP-网页跳转的方式？"><a href="#PHP-网页跳转的方式？" class="headerlink" title="PHP 网页跳转的方式？"></a>PHP 网页跳转的方式？</h1><ul><li><p>header()函数;</p><pre><code class="hljs php">header(<span class="hljs-string">'location:http://www.baidu.com'</span>);</code></pre></li><li><p>meta标签</p><pre><code class="hljs php"><span class="hljs-keyword">echo</span> <span class="hljs-string">'&lt;meta http-equiv="refresh" content="1;url=http://www.baidu.com"&gt;'</span>;</code></pre></li><li><p>script标签;</p><pre><code class="hljs php"><span class="hljs-keyword">echo</span><span class="hljs-string">'&lt;script&gt;window.location.href="http://www.baidu.com"&lt;/script&gt;'</span>;</code></pre></li></ul><h1 id="Redis-有几种类型"><a href="#Redis-有几种类型" class="headerlink" title="Redis 有几种类型?"></a>Redis 有几种类型?</h1><ul><li><strong>String（字符串）</strong> <ul><li>一个 key 对应一个 value。 </li><li>一个 key 最大能存储 512MB</li><li>常用命令：set、get、decr、incr、mget</li></ul></li><li><strong>Hash（哈希）</strong> <ul><li>键值(key =&gt; value)对集合，是一个 string 类型的 field 和 value 的映射表， 特别适合用于存储对象</li><li>每个 hash 可以存储 2<sup>32</sup>  - 1 键值对（40多亿）</li><li>常用命令：hget、hset、hgetall</li></ul></li><li><strong>List（列表）</strong> <ul><li>字符串列表，按照插入顺序排序。可以添加一个元素到列表的头部（左边）  或者尾部（右边）</li><li>列表最多可以包含 2<sup>32</sup>  - 1 个元素 (40多亿)</li><li>list 类型经常会被用于消息队列的服务，以完成多程序之间的消息交换</li><li>常用命令：lpush、rpush、lpop、rpop、lrange</li></ul></li><li><strong>Set（集合）</strong> <ul><li>Set 是 String 类型的无序集合。集合成员是唯一的，这就意味着集合中不能出现重复的数据</li><li>集合中最大的成员数为 2<sup>32</sup> - 1 (40多亿)</li><li>常用命令：sadd、spop、smembers、sunion</li></ul></li><li><strong>Sorted Set（有序集合)</strong> <ul><li>有序集合和集合一样也是 string 类型元素的集合,且不允许重复的成员</li><li>不同的是每个元素都会关联一个 double 类型的分数。redis 正是通过分数来为集合中的成员进行从小到大的排序</li><li>有序集合的成员是唯一的,但分数(score)却可以重复  </li><li>集合中最大的成员数为 2<sup>32</sup> - 1 (40多亿)</li><li>常用命令：zadd、zrange、zrem、zcard</li></ul></li></ul><h1 id="PHP-数组在底层怎么实现的？"><a href="#PHP-数组在底层怎么实现的？" class="headerlink" title="PHP 数组在底层怎么实现的？"></a>PHP 数组在底层怎么实现的？</h1><p> HashTable 是 zend 的核心数据结构，在PHP里面几乎并用来实现所有常见功能，我们知道的 PHP 数组即是其典型应用，此外，在 zend 内部，如函数符号表、全局变量等也都是基于 hash table 来实现 </p><ul><li>散列表（hashTable）具有如下特点 <ul><li>支持典型的 key =&gt; value 查询</li><li>可以当做数组使用</li><li>添加、删除节点是 O(1)复杂度</li><li>key 支持混合类型：同时存在关联数组合索引数组</li><li>Value 支持混合类型：array(“string”,2332)</li><li>支持线性遍历：如 foreach</li></ul></li></ul><h1 id="微信授权登录的流程？"><a href="#微信授权登录的流程？" class="headerlink" title="微信授权登录的流程？"></a>微信授权登录的流程？</h1><ul><li><p>调用 <a href="https://developers.weixin.qq.com/miniprogram/dev/api/open-api/login/wx.login.html" target="_blank" rel="noopener">wx.login()</a> 获取 <strong>临时登录凭证code</strong> ，并回传到开发者服务器。</p></li><li><p>调用 <a href="https://developers.weixin.qq.com/miniprogram/dev/api-backend/open-api/login/auth.code2Session.html" target="_blank" rel="noopener">auth.code2Session</a> 接口，换取 <strong>用户唯一标识 OpenID</strong> 和 <strong>会话密钥 session_key</strong>。</p></li><li><p>具体思路如下</p><ul><li>在 main.js 中封装公共函数，用于判断用户是否登录</li><li>在 main.js 中分定义全局变量，用于存储接口地址</li><li>如果没有登录、则跳转至登录页面</li><li>进入登录页面</li><li>通过 wx.login 获取用户的 code</li><li>通过 code 获取用户的 SessionKey、OpenId 等信息</li><li>通过 openId 调用后台 Api 获取用户的信息</li><li>获取成功，则说明已经授权过了,直接登录成功</li><li>获取失败，则说明没有授权过，需要授权之后才能进行登录</li><li>用户点击页面微信登录按钮 open-type=”getUserInfo”</li><li>获取用户数据，然后调用后台接口写入数据库</li></ul></li></ul><h1 id="什么是消息队列-Message-Queue-？"><a href="#什么是消息队列-Message-Queue-？" class="headerlink" title="什么是消息队列 (Message Queue) ？"></a>什么是消息队列 (Message Queue) ？</h1><ul><li><p>从本质上说消息对列就是一个队列结构的中间件，也就是说消息放入这个中间件之后就可以直接返回，并不需要系统立即处理，而另外会有一个程序读取这些数据，并按顺序进行逐次处理。</p></li><li><p>它有三个好处</p><ul><li><p><strong>异步</strong></p><ul><li>用户注册后，需要发注册邮件和注册短信。传统的做法有两种 1.串行的方式；2.并行方式</li><li>串行方式：将注册信息写入数据库成功后，发送注册邮件，再发送注册短信。以上三个任务全部完成后，返回给客户端</li><li>并行方式：将注册信息写入数据库成功后，发送注册邮件的同时，发送注册短信。以上三个任务完成后，返回给客户端。</li><li>消息队列：将注册信息写入数据库成功后，写入消息队列，异步读取需要发送的邮件和短信</li></ul></li><li><p><strong>解耦</strong></p><ul><li>订单系统：用户下单后，订单系统完成持久化处理，将消息写入消息队列，返回用户订单下单成功</li><li>库存系统：订阅下单的消息，采用拉/推的方式，获取下单信息，库存系统根据下单信息，进行库存操作</li><li>假如：在下单时库存系统不能正常使用。也不影响正常下单，因为下单后，订单系统写入消息队列就不再关心其他的后续操作了。实现订单系统与库存系统的应用解耦 </li></ul></li><li><p><strong>削峰/限流</strong></p><ul><li>秒杀活动，一般会因为流量过大，导致流量暴增，应用挂掉。为解决这个问题，一般需要在应用前端加入消息队列</li><li>可以控制活动的人数，缓解短时间内高流量压垮应用  </li><li>用户的请求，服务器接收后，首先写入消息队列。假如消息队列长度超过最大数量，则直接抛弃用户请求或跳转到错误页面 </li><li>秒杀业务根据消息队列中的请求信息，再做后续处理 </li></ul></li></ul></li></ul><ul><li>使用PHP的数组函数 <strong>array_push、array_shift</strong> 实现一个队列</li></ul><pre><code class="hljs php"><span class="hljs-meta">&lt;?php</span><span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">QueueArray</span></span><span class="hljs-class"></span>&#123;    <span class="hljs-keyword">public</span> $arr = <span class="hljs-keyword">array</span>();        <span class="hljs-keyword">public</span> <span class="hljs-function"><span class="hljs-keyword">function</span> <span class="hljs-title">tailEnqueue</span><span class="hljs-params">($val)</span></span><span class="hljs-function">    </span>&#123;        <span class="hljs-keyword">return</span> array_push(<span class="hljs-keyword">$this</span>-&gt;arr, $val); <span class="hljs-comment">// 队尾入队</span>    &#125;        <span class="hljs-keyword">public</span> <span class="hljs-function"><span class="hljs-keyword">function</span> <span class="hljs-title">tailDequeue</span><span class="hljs-params">()</span></span><span class="hljs-function">    </span>&#123;        <span class="hljs-keyword">return</span> array_pop(<span class="hljs-keyword">$this</span>-&gt;arr); <span class="hljs-comment">// 队尾出队</span>    &#125;        <span class="hljs-keyword">public</span> <span class="hljs-function"><span class="hljs-keyword">function</span> <span class="hljs-title">headEnqueue</span><span class="hljs-params">($val)</span></span><span class="hljs-function">    </span>&#123;        <span class="hljs-keyword">return</span> array_unshift(<span class="hljs-keyword">$this</span>-&gt;arr, $val); <span class="hljs-comment">// 队首入队</span>    &#125;        <span class="hljs-keyword">public</span> <span class="hljs-function"><span class="hljs-keyword">function</span> <span class="hljs-title">headDequeue</span><span class="hljs-params">()</span></span><span class="hljs-function">    </span>&#123;        <span class="hljs-keyword">return</span> array_shift(<span class="hljs-keyword">$this</span>-&gt;arr); <span class="hljs-comment">//队首出队</span>    &#125;        <span class="hljs-keyword">public</span> <span class="hljs-function"><span class="hljs-keyword">function</span> <span class="hljs-title">length</span><span class="hljs-params">()</span></span><span class="hljs-function">    </span>&#123;        <span class="hljs-keyword">return</span> count(<span class="hljs-keyword">$this</span>-&gt;arr); <span class="hljs-comment">// 队列长度</span>    &#125;        <span class="hljs-keyword">public</span> <span class="hljs-function"><span class="hljs-keyword">function</span> <span class="hljs-title">head</span><span class="hljs-params">()</span></span><span class="hljs-function">    </span>&#123;        <span class="hljs-keyword">return</span> reset(<span class="hljs-keyword">$this</span>-&gt;arr); <span class="hljs-comment">// 获取队首元素</span>    &#125;        <span class="hljs-keyword">public</span> <span class="hljs-function"><span class="hljs-keyword">function</span> <span class="hljs-title">tail</span><span class="hljs-params">()</span></span><span class="hljs-function">    </span>&#123;        <span class="hljs-keyword">return</span> end(<span class="hljs-keyword">$this</span>-&gt;arr); <span class="hljs-comment">// 获取队尾元素</span>    &#125;        <span class="hljs-keyword">public</span> <span class="hljs-function"><span class="hljs-keyword">function</span> <span class="hljs-title">clear</span><span class="hljs-params">()</span></span><span class="hljs-function">    </span>&#123;        <span class="hljs-keyword">unset</span>(<span class="hljs-keyword">$this</span>-&gt;arr); <span class="hljs-comment">// 清空队列</span>        <span class="hljs-keyword">return</span> <span class="hljs-keyword">true</span>;    &#125;&#125;</code></pre>]]></content>
    
    
    <categories>
      
      <category>PHP</category>
      
      <category>面试</category>
      
    </categories>
    
    
    <tags>
      
      <tag>PHP</tag>
      
      <tag>面试</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>PHP 设计模式之单例模式（Singleton）</title>
    <link href="/77dd5484.html"/>
    <url>/77dd5484.html</url>
    
    <content type="html"><![CDATA[<p><strong>单例模式被公认为是 反面模式，为了获得更好的可测试性和可维护性，请使用『依赖注入模式』</strong></p><h3 id="1-8-1-目的"><a href="#1-8-1-目的" class="headerlink" title="1.8.1. 目的"></a>1.8.1. 目的</h3><p>在应用程序调用的时候，只能获得一个对象实例。</p><h3 id="1-8-2-例子"><a href="#1-8-2-例子" class="headerlink" title="1.8.2. 例子"></a>1.8.2. 例子</h3><p>数据库连接<br>日志 (多种不同用途的日志也可能会成为多例模式)<br>在应用中锁定文件 (系统中只存在一个 …)</p><h3 id="1-8-4-代码部分"><a href="#1-8-4-代码部分" class="headerlink" title="1.8.4. 代码部分"></a>1.8.4. 代码部分</h3><p>Singleton.php</p><pre><code class="hljs php"><span class="hljs-meta">&lt;?php</span><span class="hljs-keyword">namespace</span> <span class="hljs-title">DesignPatterns</span>\<span class="hljs-title">Creational</span>\<span class="hljs-title">Singleton</span>;<span class="hljs-keyword">final</span> <span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">Singleton</span></span><span class="hljs-class"></span>&#123;    <span class="hljs-comment">/**</span><span class="hljs-comment">    * <span class="hljs-doctag">@var</span> Singleton</span><span class="hljs-comment">    */</span>    <span class="hljs-keyword">private</span> <span class="hljs-keyword">static</span> $instance;    <span class="hljs-comment">/**</span><span class="hljs-comment">    * 通过懒加载获得实例（在第一次使用的时候创建）</span><span class="hljs-comment">    */</span>    <span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> <span class="hljs-function"><span class="hljs-keyword">function</span> <span class="hljs-title">getInstance</span><span class="hljs-params">()</span>: <span class="hljs-title">Singleton</span></span><span class="hljs-function">    </span>&#123;        <span class="hljs-keyword">if</span> (<span class="hljs-keyword">null</span> === <span class="hljs-keyword">static</span>::$instance) &#123;            <span class="hljs-keyword">static</span>::$instance = <span class="hljs-keyword">new</span> <span class="hljs-keyword">static</span>();        &#125;        <span class="hljs-keyword">return</span> <span class="hljs-keyword">static</span>::$instance;    &#125;    <span class="hljs-comment">/**</span><span class="hljs-comment">    * 不允许从外部调用以防止创建多个实例</span><span class="hljs-comment">    * 要使用单例，必须通过 Singleton::getInstance() 方法获取实例</span><span class="hljs-comment">    */</span>    <span class="hljs-keyword">private</span> <span class="hljs-function"><span class="hljs-keyword">function</span> <span class="hljs-title">__construct</span><span class="hljs-params">()</span></span><span class="hljs-function">    </span>&#123;    &#125;    <span class="hljs-comment">/**</span><span class="hljs-comment">    * 防止实例被克隆（这会创建实例的副本）</span><span class="hljs-comment">    */</span>    <span class="hljs-keyword">private</span> <span class="hljs-function"><span class="hljs-keyword">function</span> <span class="hljs-title">__clone</span><span class="hljs-params">()</span></span><span class="hljs-function">    </span>&#123;    &#125;    <span class="hljs-comment">/**</span><span class="hljs-comment">    * 防止反序列化（这将创建它的副本）</span><span class="hljs-comment">    */</span>    <span class="hljs-keyword">private</span> <span class="hljs-function"><span class="hljs-keyword">function</span> <span class="hljs-title">__wakeup</span><span class="hljs-params">()</span></span><span class="hljs-function">    </span>&#123;    &#125;&#125;</code></pre><h3 id="1-8-5-测试"><a href="#1-8-5-测试" class="headerlink" title="1.8.5. 测试"></a>1.8.5. 测试</h3><p>Tests/SingletonTest.php</p><pre><code class="hljs php"><span class="hljs-meta">&lt;?php</span><span class="hljs-keyword">namespace</span> <span class="hljs-title">DesignPatterns</span>\<span class="hljs-title">Creational</span>\<span class="hljs-title">Singleton</span>\<span class="hljs-title">Tests</span>;<span class="hljs-keyword">use</span> <span class="hljs-title">DesignPatterns</span>\<span class="hljs-title">Creational</span>\<span class="hljs-title">Singleton</span>\<span class="hljs-title">Singleton</span>;<span class="hljs-keyword">use</span> <span class="hljs-title">PHPUnit</span>\<span class="hljs-title">Framework</span>\<span class="hljs-title">TestCase</span>;<span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">SingletonTest</span> <span class="hljs-keyword">extends</span> <span class="hljs-title">TestCase</span></span><span class="hljs-class"></span>&#123;    <span class="hljs-keyword">public</span> <span class="hljs-function"><span class="hljs-keyword">function</span> <span class="hljs-title">testUniqueness</span><span class="hljs-params">()</span></span><span class="hljs-function">    </span>&#123;        $firstCall = Singleton::getInstance();        $secondCall = Singleton::getInstance();        <span class="hljs-keyword">$this</span>-&gt;assertInstanceOf(Singleton::class, $firstCall);        <span class="hljs-keyword">$this</span>-&gt;assertSame($firstCall, $secondCall);    &#125;&#125;</code></pre><p>————————————————<br>原文作者：PHP 技术论坛文档：《PHP 设计模式全集（2018）》<br>转自链接：<a href="https://learnku.com/docs/php-design-patterns/2018/Singleton/1494" target="_blank" rel="noopener">https://learnku.com/docs/php-design-patterns/2018/Singleton/1494</a><br>版权声明：翻译文档著作权归译者和 LearnKu 社区所有</p>]]></content>
    
    
    <categories>
      
      <category>设计模式</category>
      
      <category>PHP</category>
      
    </categories>
    
    
    <tags>
      
      <tag>创建型模式</tag>
      
      <tag>PHP</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>在 PHP 中使用 cURL</title>
    <link href="/b4a5aa5.html"/>
    <url>/b4a5aa5.html</url>
    
    <content type="html"><![CDATA[<h1 id="什么是cURL"><a href="#什么是cURL" class="headerlink" title="什么是cURL"></a>什么是cURL</h1><ul><li><p>先来看下维基百科的解释：</p><pre><code>cURL 是一个开源项目，主要的产品是 curl（命令行工具）和 libcurl（C语言的API库），两者功能均是：基于网络协议，对指定 URL 进行网络传输。cURL 涉及是任何网络协议传输，不涉及对具体数据的具体处理。（如：html 的渲染等）</code></pre></li><li><p>简单讲就是我们不用浏览器，在curl命令行工具就可以访问任意网址</p></li><li><p>PHP中集成了cURL库，那么我们就可以在代码上使用它的功能</p></li></ul><h1 id="在PHP中使用cURL"><a href="#在PHP中使用cURL" class="headerlink" title="在PHP中使用cURL"></a>在PHP中使用cURL</h1><p>  使用 cURL 函数的基本思想是先使用 <strong>curl_init()</strong> 初始化 cURL 会话，接着可以通过 <strong>curl_setopt()</strong> 设置需要的全部选项，然后使用 <strong>curl_exec()</strong> 来执行会话，当执行完会话后使用 <strong>curl_close()</strong> 关闭会话</p><ul><li><p>其中最需要我们配置的是 curl_setopt() 这步,我们来看下这个函数的说明</p><pre><code class="hljs php"><span class="hljs-comment">/**</span><span class="hljs-comment">*  为 cURL 会话句柄设置选项。</span><span class="hljs-comment">*  <span class="hljs-doctag">@param</span> resource $ch 返回的 cURL 句柄</span><span class="hljs-comment">*  <span class="hljs-doctag">@param</span> int $option 需要设置的CURLOPT_XXX选项。</span><span class="hljs-comment">*  <span class="hljs-doctag">@param</span> mixed $value 将设置在option选项上的值，可以接收多种类型的参数。</span><span class="hljs-comment">*  <span class="hljs-doctag">@return</span> bool 成功时返回 TRUE， 或者在失败时返回 FALSE </span><span class="hljs-comment">*/</span>curl_setopt( resource $ch, int $option, mixed $value) : bool<span class="hljs-comment">/*</span><span class="hljs-comment"> * 为 cURL 传输会话批量设置选项</span><span class="hljs-comment"> * <span class="hljs-doctag">@param</span> resource $ch 返回的 cURL 句柄</span><span class="hljs-comment"> * <span class="hljs-doctag">@param</span> array $options 一个 array 用来确定将被设置的选项及其值。数组的键值必须是一个有效的curl_setopt()常量或者是它们对等的整数值。 </span><span class="hljs-comment"> */</span>curl_setopt_array( resource $ch, <span class="hljs-keyword">array</span> $options) : bool</code></pre></li><li><p><a href="https://www.php.net/curl_setopt" target="_blank" rel="noopener">option参数选项和值具体看官方文档，用的时候在查询</a></p></li><li><p>我们看下官方文档中使用 cURL 发送 POST 和 GET 封装后的函数</p><pre><code class="hljs php"><span class="hljs-comment">/** </span><span class="hljs-comment">* Send a POST requst using cURL                              </span><span class="hljs-comment">* <span class="hljs-doctag">@param</span> string $url to request </span><span class="hljs-comment">* <span class="hljs-doctag">@param</span> array $post values to send </span><span class="hljs-comment">* <span class="hljs-doctag">@param</span> array $options for cURL </span><span class="hljs-comment">* <span class="hljs-doctag">@return</span> string </span><span class="hljs-comment">*/</span> <span class="hljs-function"><span class="hljs-keyword">function</span> <span class="hljs-title">curl_post</span><span class="hljs-params">($url, array $post = array<span class="hljs-params">()</span>, array $options = array<span class="hljs-params">()</span>)</span> </span><span class="hljs-function"></span>&#123;     $defaults = <span class="hljs-keyword">array</span>(         CURLOPT_POST =&gt; <span class="hljs-number">1</span>, <span class="hljs-comment">// 发送 POST 请求</span>        CURLOPT_HEADER =&gt; <span class="hljs-number">0</span>, <span class="hljs-comment">// 启用时会将头文件的信息作为数据流输出，这边不启用。 </span>        CURLOPT_URL =&gt; $url, <span class="hljs-comment">// 需要获取的 URL 地址，也可以在curl_init() 初始化会话的时候</span>        CURLOPT_FRESH_CONNECT =&gt; <span class="hljs-number">1</span>, <span class="hljs-comment">// 强制获取一个新的连接，而不是缓存中的连接</span>        CURLOPT_RETURNTRANSFER =&gt; <span class="hljs-number">1</span>, <span class="hljs-comment">// 将curl_exec()获取的信息以字符串返回，而不是直接输出。</span>        CURLOPT_FORBID_REUSE =&gt; <span class="hljs-number">1</span>, <span class="hljs-comment">// 在完成交互以后强制明确的断开连接，不能在连接池中重用</span>        <span class="hljs-comment">// CURLOPT_TIMEOUT =&gt; 10, // 允许 cURL 函数执行的最长秒数，这边先不限制</span>        CURLOPT_POSTFIELDS =&gt; http_build_query($post), <span class="hljs-comment">// 全部数据使用HTTP协议中的 "POST" 操作来发送，http_build_query返回一个 URL 编码后的字符串</span>        <span class="hljs-comment">// 以下两个设置是为了防止访问没有SSL证书的网站报错</span>        CURLOPT_SSL_VERIFYPEER =&gt; <span class="hljs-number">0</span>, <span class="hljs-comment">// 禁止 cURL 验证对等证书（peer'scertificate）。要验证的交换证书可以在 CURLOPT_CAINFO 选项中设置，或在 CURLOPT_CAPATH中设置证书目录。</span>        CURLOPT_SSL_VERIFYHOST =&gt; <span class="hljs-number">0</span> <span class="hljs-comment">// 设置为 1 是检查服务器SSL证书中是否存在一个公用名, 0 为不检查名称</span>    );     $ch = curl_init();     curl_setopt_array($ch, ($options + $defaults));    <span class="hljs-keyword">if</span>( ! $result = curl_exec($ch))     &#123;         trigger_error(curl_error($ch));     &#125;     curl_close($ch);     <span class="hljs-keyword">return</span> $result; &#125; <span class="hljs-comment">/** </span><span class="hljs-comment">* Send a GET requst using cURL </span><span class="hljs-comment">* <span class="hljs-doctag">@param</span> string $url to request </span><span class="hljs-comment">* <span class="hljs-doctag">@param</span> array $get values to send </span><span class="hljs-comment">* <span class="hljs-doctag">@param</span> array $options for cURL </span><span class="hljs-comment">* <span class="hljs-doctag">@return</span> string </span><span class="hljs-comment">*/</span> <span class="hljs-function"><span class="hljs-keyword">function</span> <span class="hljs-title">curl_get</span><span class="hljs-params">($url, array $get = array<span class="hljs-params">()</span>, array $options = array<span class="hljs-params">()</span>)</span> </span><span class="hljs-function"></span>&#123;        $defaults = <span class="hljs-keyword">array</span>(         CURLOPT_URL =&gt; $url. (strpos($url, <span class="hljs-string">'?'</span>) === <span class="hljs-keyword">FALSE</span> ? <span class="hljs-string">'?'</span> : <span class="hljs-string">''</span>). http_build_query($get), <span class="hljs-comment">// 需要获取的 URL 地址，也可以在curl_init() 初始化会话的时候。 </span>        CURLOPT_HEADER =&gt; <span class="hljs-number">0</span>, <span class="hljs-comment">// 启用时会将头文件的信息作为数据流输出。 </span>        CURLOPT_RETURNTRANSFER =&gt; <span class="hljs-keyword">TRUE</span>,         <span class="hljs-comment">// CURLOPT_TIMEOUT =&gt; 10,</span>        CURLOPT_SSL_VERIFYPEER =&gt; <span class="hljs-number">0</span>,         CURLOPT_SSL_VERIFYHOST =&gt; <span class="hljs-number">0</span>                );         $ch = curl_init();     curl_setopt_array($ch, ($options + $defaults));     <span class="hljs-keyword">if</span>( ! $result = curl_exec($ch))     &#123;         trigger_error(curl_error($ch));     &#125;     curl_close($ch);     <span class="hljs-keyword">return</span> $result; &#125;</code></pre><h1 id="发送GET-POST请求到接口交互数据"><a href="#发送GET-POST请求到接口交互数据" class="headerlink" title="发送GET/POST请求到接口交互数据"></a>发送GET/POST请求到接口交互数据</h1></li></ul><ul><li><p>首先我们得有个运行PHP的环境，我这边使用 PHPStudy</p></li><li><p>其次在站点域名那里配置我们测试的 demo 的域名</p></li></ul><p><img src="https://cdn.jsdelivr.net/gh/loquy/loquy.github.io/images/1591845702.jpg" srcset="/img/loading.gif" alt="phpStudy"></p><ul><li><p>以下为测试 demo</p><p>新建两个 php 文件：一个是提供数据的接口，创建一个数组然后转成 json 格式输出到浏览器；另个是使用 curl 获取数据的接口，使用上面封装好的 curl_get 和 curl_post 函数获取数据。</p></li></ul><pre><code class="hljs php"><span class="hljs-meta">&lt;?php</span><span class="hljs-comment">/**</span><span class="hljs-comment"> * 提供数据的类，</span><span class="hljs-comment"> */</span><span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">Data</span></span><span class="hljs-class"></span>&#123;    <span class="hljs-keyword">public</span> <span class="hljs-function"><span class="hljs-keyword">function</span> <span class="hljs-title">creatData</span><span class="hljs-params">()</span></span><span class="hljs-function">    </span>&#123;        $data = [<span class="hljs-string">'car'</span>=&gt;<span class="hljs-string">'Benz'</span>,<span class="hljs-string">'book'</span>=&gt;<span class="hljs-string">'Little Prince'</span>,<span class="hljs-string">'house'</span>=&gt;<span class="hljs-string">'The Ocean Villas'</span>];        <span class="hljs-keyword">echo</span> json_encode($data);    &#125;&#125;$data = <span class="hljs-keyword">new</span> Data;$data-&gt;creatData();</code></pre><pre><code class="hljs php"><span class="hljs-meta">&lt;?php</span><span class="hljs-comment">/**</span><span class="hljs-comment">* 获取数据的类</span><span class="hljs-comment">*/</span><span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">GetData</span></span><span class="hljs-class"></span>&#123;    <span class="hljs-keyword">public</span> $url = <span class="hljs-string">'http://127.0.0.1:1000/data.php'</span>;    <span class="hljs-keyword">public</span> <span class="hljs-function"><span class="hljs-keyword">function</span> <span class="hljs-title">test_curl_get</span><span class="hljs-params">()</span></span><span class="hljs-function">    </span>&#123;        $result = <span class="hljs-keyword">$this</span>-&gt;curl_get(<span class="hljs-keyword">$this</span>-&gt;url);        var_dump(json_decode($result,<span class="hljs-keyword">true</span>));    &#125;    <span class="hljs-keyword">public</span> <span class="hljs-function"><span class="hljs-keyword">function</span> <span class="hljs-title">test_curl_post</span><span class="hljs-params">()</span></span><span class="hljs-function">    </span>&#123;        $result = <span class="hljs-keyword">$this</span>-&gt;curl_post(<span class="hljs-keyword">$this</span>-&gt;url);        var_dump(json_decode($result,<span class="hljs-keyword">true</span>));    &#125;        <span class="hljs-comment">/**</span><span class="hljs-comment">    * Send a POST requst using cURL</span><span class="hljs-comment">    * <span class="hljs-doctag">@param</span> string $url to request</span><span class="hljs-comment">    * <span class="hljs-doctag">@param</span> array $post values to send</span><span class="hljs-comment">    * <span class="hljs-doctag">@param</span> array $options for cURL</span><span class="hljs-comment">    * <span class="hljs-doctag">@return</span> string</span><span class="hljs-comment">    */</span>    <span class="hljs-keyword">public</span> <span class="hljs-function"><span class="hljs-keyword">function</span> <span class="hljs-title">curl_post</span><span class="hljs-params">($url, array $post = array<span class="hljs-params">()</span>, array $options = array<span class="hljs-params">()</span>)</span></span><span class="hljs-function">    </span>&#123;        $defaults = <span class="hljs-keyword">array</span>(            CURLOPT_POST =&gt; <span class="hljs-number">1</span>, <span class="hljs-comment">// 发送 POST 请求</span>            CURLOPT_HEADER =&gt; <span class="hljs-number">0</span>, <span class="hljs-comment">// 启用时会将头文件的信息作为数据流输出，这边不启用。</span>            CURLOPT_URL =&gt; $url, <span class="hljs-comment">// 需要获取的 URL 地址，也可以在curl_init() 初始化会话的时候</span>            CURLOPT_FRESH_CONNECT =&gt; <span class="hljs-number">1</span>, <span class="hljs-comment">// 强制获取一个新的连接，而不是缓存中的连接</span>            CURLOPT_RETURNTRANSFER =&gt; <span class="hljs-number">1</span>, <span class="hljs-comment">// 将curl_exec()获取的信息以字符串返回，而不是直接输出。</span>            CURLOPT_FORBID_REUSE =&gt; <span class="hljs-number">1</span>, <span class="hljs-comment">// 在完成交互以后强制明确的断开连接，不能在连接池中重用</span>            <span class="hljs-comment">// CURLOPT_TIMEOUT =&gt; 10, // 允许 cURL 函数执行的最长秒数，这边先不限制</span>            CURLOPT_POSTFIELDS =&gt; http_build_query($post), <span class="hljs-comment">// 全部数据使用HTTP协议中的 "POST" 操作来发送，http_build_query返回一个 URL 编码后的字符串</span>            <span class="hljs-comment">// 以下两个设置是为了防止访问没有SSL证书的网站报错</span>            CURLOPT_SSL_VERIFYPEER =&gt; <span class="hljs-number">0</span>, <span class="hljs-comment">// 禁止 cURL 验证对等证书（peer'scertificate）。要验证的交换证书可以在 CURLOPT_CAINFO 选项中设置，或在 CURLOPT_CAPATH中设置证书目录。</span>            CURLOPT_SSL_VERIFYHOST =&gt; <span class="hljs-number">0</span> <span class="hljs-comment">// 设置为 1 是检查服务器SSL证书中是否存在一个公用名, 0 为不检查名称</span>        );        $ch = curl_init();        curl_setopt_array($ch, ($options + $defaults));        <span class="hljs-keyword">if</span> (! $result = curl_exec($ch)) &#123;            trigger_error(curl_error($ch));        &#125;        curl_close($ch);        <span class="hljs-keyword">return</span> $result;    &#125;    <span class="hljs-comment">/**</span><span class="hljs-comment">    * Send a GET requst using cURL</span><span class="hljs-comment">    * <span class="hljs-doctag">@param</span> string $url to request</span><span class="hljs-comment">    * <span class="hljs-doctag">@param</span> array $get values to send</span><span class="hljs-comment">    * <span class="hljs-doctag">@param</span> array $options for cURL</span><span class="hljs-comment">    * <span class="hljs-doctag">@return</span> string</span><span class="hljs-comment">    */</span>    <span class="hljs-keyword">public</span> <span class="hljs-function"><span class="hljs-keyword">function</span> <span class="hljs-title">curl_get</span><span class="hljs-params">($url, array $get = array<span class="hljs-params">()</span>, array $options = array<span class="hljs-params">()</span>)</span></span><span class="hljs-function">    </span>&#123;        $defaults = <span class="hljs-keyword">array</span>(            CURLOPT_URL =&gt; $url. (strpos($url, <span class="hljs-string">'?'</span>) === <span class="hljs-keyword">false</span> ? <span class="hljs-string">'?'</span> : <span class="hljs-string">''</span>). http_build_query($get), <span class="hljs-comment">// 需要获取的 URL 地址，也可以在curl_init() 初始化会话的时候。</span>            CURLOPT_HEADER =&gt; <span class="hljs-number">0</span>, <span class="hljs-comment">// 启用时会将头文件的信息作为数据流输出。</span>            CURLOPT_RETURNTRANSFER =&gt; <span class="hljs-keyword">true</span>,            <span class="hljs-comment">// CURLOPT_TIMEOUT =&gt; 10,</span>            CURLOPT_SSL_VERIFYPEER =&gt; <span class="hljs-number">0</span>,            CURLOPT_SSL_VERIFYHOST =&gt; <span class="hljs-number">0</span>        );                $ch = curl_init();        curl_setopt_array($ch, ($options + $defaults));        <span class="hljs-keyword">if</span> (! $result = curl_exec($ch)) &#123;            trigger_error(curl_error($ch));        &#125;        curl_close($ch);        <span class="hljs-keyword">return</span> $result;    &#125;&#125;$GetData = <span class="hljs-keyword">new</span> GetData;$GetData-&gt;test_curl_get();$GetData-&gt;test_curl_post();</code></pre><ul><li>在浏览器访问写好的 demo</li></ul><p><img src="https://cdn.jsdelivr.net/gh/loquy/loquy.github.io/images/1591846206.jpg" srcset="/img/loading.gif" alt="phpStudy"></p><p><img src="https://cdn.jsdelivr.net/gh/loquy/loquy.github.io/images/1591846227.jpg" srcset="/img/loading.gif" alt="phpStudy"></p><p><strong>到此便使用curl成功获取到数据</strong></p>]]></content>
    
    
    <categories>
      
      <category>PHP</category>
      
    </categories>
    
    
    <tags>
      
      <tag>PHP</tag>
      
      <tag>cURL</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>使用 Github Pages 和 Hexo 搭建博客</title>
    <link href="/3b0a6c12.html"/>
    <url>/3b0a6c12.html</url>
    
    <content type="html"><![CDATA[<h1 id="项目简介"><a href="#项目简介" class="headerlink" title="项目简介"></a>项目简介</h1><p>当前项目是使用 <a href="https://pages.github.com/" target="_blank" rel="noopener">Github Pages</a> 和 <a href="https://Hexo.io/zh-cn/docs/" target="_blank" rel="noopener">Hexo</a> 搭建的静态博客</p><pre><code>Github Pages 是 GitHub 提供的一个网页寄存服务，于 2008 年推出。可以用于存放静态网页，包括博客、项目文档甚至整本书。Jekyll 软件可以用于将文档转换成静态网页，该软件提供了将网页上传到 Github Pages 的功能。一般 Github Pages 的网站使用 github.io 的子域名，但是用户也可以使用第三方域名。Hexo 是一个快速、简洁且高效的博客框架。Hexo 使用 Markdown（或其他渲染引擎）解析文章，在几秒内，即可利用靓丽的主题生成静态网页。</code></pre><ul><li>首先搭建 Github Pages</li><li>然后使用 Hexo 部署到 Github 远程仓库</li></ul><h2 id="Github-Pages-的搭建方式"><a href="#Github-Pages-的搭建方式" class="headerlink" title="Github Pages 的搭建方式"></a>Github Pages 的搭建方式</h2><p>如果之前未使用过 Git，可以学习，<a href="http://rogerdudler.github.io/git-guide/index.zh.html" target="_blank" rel="noopener">Git 小白教程</a></p><ul><li>创建名为 username.github.io 的仓库，username 是你的 github 的用户名，不是昵称</li><li>克隆到本地<br>  git clone <a href="https://github.com/username/username.github.io" target="_blank" rel="noopener">https://github.com/username/username.github.io</a></li><li>进入文件夹创建 index.html 文件</li><li>推送到 github 远程仓库<br>  git add –all<br>  git commit -m “Initial commit”<br>  git push -u origin master</li><li>搭建完成，在浏览器打开 <a href="https://username.github.io" target="_blank" rel="noopener">https://username.github.io</a> 即可访问</li></ul><h2 id="Hexo-安装"><a href="#Hexo-安装" class="headerlink" title="Hexo 安装"></a>Hexo 安装</h2><h3 id="安装前提"><a href="#安装前提" class="headerlink" title="安装前提"></a>安装前提</h3><p>安装 hexo 相当简单，只需要先安装下列应用程序即可：</p><ul><li><a href="https://nodejs.org/en/" target="_blank" rel="noopener">Node.js</a></li><li><a href="https://git-scm.com/" target="_blank" rel="noopener">Git</a></li></ul><h3 id="安装-Hexo"><a href="#安装-Hexo" class="headerlink" title="安装 Hexo"></a>安装 Hexo</h3><p>所有必备的应用程序安装完成后，即可使用 npm 安装 hexo。</p><pre><code>$ npm install -g hexo-cli</code></pre><p>对于熟悉 npm 的进阶用户，可以仅局部安装 Hexo 包。</p><pre><code>$ npm install hexo</code></pre><h3 id="新建一个网站和启动服务器运行"><a href="#新建一个网站和启动服务器运行" class="headerlink" title="新建一个网站和启动服务器运行"></a>新建一个网站和启动服务器运行</h3><pre><code>$ hexo init &lt;folder&gt; 如果没有设置 folder hexo 默认在目前的文件夹建立网站$ hexo server 简写 $ hexo s 默认情况下，访问网址为： http://localhost:4000/</code></pre><h3 id="部署到-Github-Pages"><a href="#部署到-Github-Pages" class="headerlink" title="部署到 Github Pages"></a>部署到 Github Pages</h3><ul><li><p>编辑 <strong>_config.yml</strong> 配置文件</p><pre><code>deploy:    type: git    repo: https://github.com/username/username.github.io.git    branch: master  </code></pre></li><li><p><strong>hexo generate</strong> 生成静态文件。</p></li><li><p><strong>hexo deploy</strong></p></li><li><p>以上两步可以简写为 <strong>hexo g -d</strong> 或 <strong>hexo d -g</strong></p></li></ul><h2 id="Hexo-目录结构"><a href="#Hexo-目录结构" class="headerlink" title="Hexo 目录结构"></a>Hexo 目录结构</h2><pre><code>.├── _config.yml ├── package.json├── scaffolds├── source|   ├── _drafts|   └── _posts└── themes└── node_modules└── public└── .deploy_git</code></pre><h3 id="config-yml"><a href="#config-yml" class="headerlink" title="_config.yml"></a>_config.yml</h3><p><a href="https://Hexo.io/zh-cn/docs/configuration" target="_blank" rel="noopener">Hexo配置文件</a></p><h3 id="package-json"><a href="#package-json" class="headerlink" title="package.json"></a>package.json</h3><p>应用程序的信息</p><h3 id="scaffolds"><a href="#scaffolds" class="headerlink" title="scaffolds"></a>scaffolds</h3><p>模版件夹。当您新建文章时，Hexo 会根据 scaffold 来建立文件。Hexo 的模板是指在新建的文章文件中默认填充的内容。</p><h3 id="source"><a href="#source" class="headerlink" title="source"></a>source</h3><p>资源文件夹是存放用户资源的地方。除 _posts 文件夹之外，开头命名为 _ (下划线)的文件 / 文件夹和隐藏的文件将会被忽略。Markdown 和 HTML 文件会被解析并放到 public 文件夹，而其他文件会被拷贝过去。</p><h3 id="themes"><a href="#themes" class="headerlink" title="themes"></a>themes</h3><p>主题文件夹。Hexo 会根据主题来生成静态页面。</p><h3 id="特殊目录"><a href="#特殊目录" class="headerlink" title="特殊目录"></a>特殊目录</h3><ul><li>node_modules， <strong>npm install hexo</strong> 生成的包和依赖</li><li>public ，<strong>hexo g</strong> 部署之前预先生成的静态文件</li><li>.deploy_git， <strong>hexo d</strong> 部署提交到 git 主干的目录</li></ul><h2 id="Hexo-发布文章"><a href="#Hexo-发布文章" class="headerlink" title="Hexo 发布文章"></a>Hexo 发布文章</h2><ul><li>创建文章 <strong>hexo new “article title”</strong></li><li>本地预览 <strong>hexo server</strong></li><li>部署网站 <strong>hexo g -d</strong> 或 <strong>hexo d -g</strong></li></ul><h2 id="多台电脑同步-Hexo-版本"><a href="#多台电脑同步-Hexo-版本" class="headerlink" title="多台电脑同步 Hexo 版本"></a>多台电脑同步 Hexo 版本</h2><p><strong>问题：远程仓库存的是静态文件，换电脑就无法使用 hexo</strong><br><strong>解决思路：创建分支管理 hexo 源代码</strong></p><ul><li><p>克隆 username.github.io 仓库到本地</p><pre><code>git clone https://github.com/username/username.github.io.git</code></pre></li><li><p>除 git 外的文件都删掉</p></li><li><p>整个 hexo 文件复制过来</p></li><li><p>切换并创建一个叫 hexo 的分支</p><pre><code>git checkout -b Hexo</code></pre></li><li><p>提交到 hexo 分支</p><pre><code>git add .git commit -m &quot;新建分支&quot;git remote add origin https://github.com/username/username.github.io.gitgit push -u origin hexo</code></pre></li></ul>]]></content>
    
    
    <categories>
      
      <category>博客搭建</category>
      
    </categories>
    
    
    <tags>
      
      <tag>Hexo</tag>
      
      <tag>Github Pages</tag>
      
      <tag>博客搭建</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>Vue 基础知识总结</title>
    <link href="/1cd82901.html"/>
    <url>/1cd82901.html</url>
    
    <content type="html"><![CDATA[<h6 id="一、常用指令"><a href="#一、常用指令" class="headerlink" title="一、常用指令"></a>一、常用指令</h6><ul><li>v-on 绑定一个事件 缩写 @ 在 methods 类方法里面操作绑定的方法</li><li>v-bind 绑定一个属性 缩写 : 在 data 数据对象里面操作绑定的数据</li><li>v-model 双向绑定通常用于获取 input 框输入的内容，无论那边更改都会同步值</li><li>v-for 基于数组或对象来循环渲染一个列表，语法：<br>1.循环数组，index 是数组的索引 <pre><code class="hljs javascript">v-<span class="hljs-keyword">for</span>=<span class="hljs-string">"(item,index) in items"</span> :key=<span class="hljs-string">"index"</span></code></pre> 2.循环对象，name 是对象的键值  <pre><code class="hljs javascript">v-<span class="hljs-keyword">for</span>=<span class="hljs-string">"(value, name, index) in object"</span> :key=<span class="hljs-string">"index"</span></code></pre> 3.为什么使用 v-for 时必须添加唯一的 key?<pre><code>使用 v-for 更新已渲染的元素列表时，默认用就地复用策略;列表数据修改的时候，他会根据 key 值去判断某个值是否修改，如果修改，则重新渲染这一项，否则复用之前的元素</code></pre></li><li>v-if v-else v-else-if 动态添加或删除元素</li><li>v-show 添加元素的 display 样式控制显示或隐藏</li><li>v-html 输出 html 代码</li></ul><h6 id="二、常用属性"><a href="#二、常用属性" class="headerlink" title="二、常用属性"></a>二、常用属性</h6><table><thead><tr><th align="left">属性写法</th><th align="left">名称</th><th align="left">作用</th></tr></thead><tbody><tr><td align="left">data: {}</td><td align="left">数据属性</td><td align="left">用来存放需要绑定的数据</td></tr><tr><td align="left">methods: {}</td><td align="left">事件操作属性</td><td align="left">用来放定义的事件方法</td></tr><tr><td align="left">components: {}</td><td align="left">组件属性</td><td align="left">自定义组件声明</td></tr><tr><td align="left">props: {}</td><td align="left">获取父组件数据</td><td align="left">单向绑定</td></tr><tr><td align="left">computed: {}</td><td align="left">计算属性</td><td align="left">提供相对简单的数据计算</td></tr><tr><td align="left">watch: {}</td><td align="left">侦听器</td><td align="left">观察某一特定question的值</td></tr><tr><td align="left">directives: {}</td><td align="left">自定义指令属性</td><td align="left">注册内部自定义指令</td></tr><tr><td align="left">filters: {}</td><td align="left">过滤器</td><td align="left">自定义过滤器</td></tr><tr><td align="left">…</td><td align="left"></td><td align="left"></td></tr></tbody></table><h6 id="三、文件结构"><a href="#三、文件结构" class="headerlink" title="三、文件结构"></a>三、文件结构</h6> <pre><code class="hljs javascript">&lt;template&gt;  &lt;div <span class="hljs-class"><span class="hljs-keyword">class</span></span>=<span class="hljs-string">"example"</span>&gt;&#123;&#123; msg &#125;&#125;&lt;<span class="hljs-regexp">/div&gt;</span><span class="hljs-regexp">&lt;/</span>template&gt;&lt;script&gt;<span class="hljs-keyword">import</span> otherComponent <span class="hljs-keyword">from</span> <span class="hljs-string">'./OtherComponent.vue'</span><span class="hljs-keyword">export</span> <span class="hljs-keyword">default</span> &#123;    <span class="hljs-comment">// 常用属性</span>    components: &#123;      <span class="hljs-comment">// 组件注册</span>      otherComponent    &#125;,    data () &#123;       <span class="hljs-keyword">return</span> &#123;        <span class="hljs-comment">// 自定义数据</span>        msg: <span class="hljs-string">'Hello world!'</span>      &#125;    &#125;,        create () &#123;      <span class="hljs-comment">// 初始化</span>    &#125;,    mounted () &#123;      <span class="hljs-comment">// 挂载</span>      <span class="hljs-keyword">this</span>.getData()    &#125;,    methods: &#123;      <span class="hljs-comment">// 自定义方法</span>      getData()&#123;          <span class="hljs-built_in">console</span>.log(<span class="hljs-string">'success'</span>)      &#125;    &#125;&#125;&lt;<span class="hljs-regexp">/script&gt;</span><span class="hljs-regexp"></span><span class="hljs-regexp">&lt;style&gt;</span><span class="hljs-regexp">  .example &#123;</span><span class="hljs-regexp">    color: red;</span><span class="hljs-regexp">  &#125;</span><span class="hljs-regexp">&lt;/</span>style&gt;</code></pre><h6 id="四、生命周期"><a href="#四、生命周期" class="headerlink" title="四、生命周期"></a>四、生命周期</h6><p><img src="https://cn.vuejs.org/images/lifecycle.png" srcset="/img/loading.gif" alt="avatar"></p>]]></content>
    
    
    <categories>
      
      <category>Vue</category>
      
    </categories>
    
    
    <tags>
      
      <tag>Vue</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>Hello Hexo</title>
    <link href="/a1751c09.html"/>
    <url>/a1751c09.html</url>
    
    <content type="html"><![CDATA[<p>Welcome to <a href="https://hexo.io/" target="_blank" rel="noopener">Hexo</a>! This is your very first post. Check <a href="https://hexo.io/docs/" target="_blank" rel="noopener">documentation</a> for more info. If you get any problems when using Hexo, you can find the answer in <a href="https://hexo.io/docs/troubleshooting.html" target="_blank" rel="noopener">troubleshooting</a> or you can ask me on <a href="https://github.com/hexojs/hexo/issues" target="_blank" rel="noopener">GitHub</a>.</p><h2 id="Quick-Start"><a href="#Quick-Start" class="headerlink" title="Quick Start"></a>Quick Start</h2><h3 id="Create-a-new-post"><a href="#Create-a-new-post" class="headerlink" title="Create a new post"></a>Create a new post</h3><pre><code class="hljs bash">$ hexo new <span class="hljs-string">"My New Post"</span></code></pre><p>More info: <a href="https://hexo.io/docs/writing.html" target="_blank" rel="noopener">Writing</a></p><h3 id="Run-server"><a href="#Run-server" class="headerlink" title="Run server"></a>Run server</h3><pre><code class="hljs bash">$ hexo server</code></pre><p>More info: <a href="https://hexo.io/docs/server.html" target="_blank" rel="noopener">Server</a></p><h3 id="Generate-static-files"><a href="#Generate-static-files" class="headerlink" title="Generate static files"></a>Generate static files</h3><pre><code class="hljs bash">$ hexo generate</code></pre><p>More info: <a href="https://hexo.io/docs/generating.html" target="_blank" rel="noopener">Generating</a></p><h3 id="Deploy-to-remote-sites"><a href="#Deploy-to-remote-sites" class="headerlink" title="Deploy to remote sites"></a>Deploy to remote sites</h3><pre><code class="hljs bash">$ hexo deploy</code></pre><p>More info: <a href="https://hexo.io/docs/one-command-deployment.html" target="_blank" rel="noopener">Deployment</a></p>]]></content>
    
    
    <categories>
      
      <category>Hexo</category>
      
    </categories>
    
    
    <tags>
      
      <tag>Hexo</tag>
      
    </tags>
    
  </entry>
  
  
  
  
</search>
